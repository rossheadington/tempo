# Tempo configuration — EXAMPLE / TEMPLATE.
#
# Copy this file to `.env` (which is gitignored) and fill in real values.
# NEVER put real secrets in this file or commit a real `.env`. This is a PUBLIC
# repo. Settings are read by tempo/config.py via pydantic-settings; every key is
# prefixed with TEMPO_ and can also be set as an environment variable.

# ---- Runtime data location ----------------------------------------------------
# Root directory for ALL local runtime data: the SQLite DB, OAuth tokens, and
# generated reports. Defaults to ~/.tempo/ — deliberately OUTSIDE the repo tree
# so an accidental `git add .` can never sweep up a secret or health data.
# Uncomment to override:
# TEMPO_DATA_DIR=~/.tempo

# ---- Strava OAuth (Phase 2) ---------------------------------------------------
# Create an API application at https://www.strava.com/settings/api
TEMPO_STRAVA_CLIENT_ID=
TEMPO_STRAVA_CLIENT_SECRET=

# ---- Garmin Connect (Phase 6) -------------------------------------------------
# Your personal Garmin Connect login. Used by the unofficial garminconnect
# library; tokens are persisted under TEMPO_DATA_DIR/tokens after first login.
TEMPO_GARMIN_EMAIL=
TEMPO_GARMIN_PASSWORD=
