# Plan 17-03: Config + analysis wire-up

**Wave:** 17-03 (sequential after 17-01 and 17-02 merged to main)
**Status:** Ready
**Depends on:** 17-01, 17-02
**Blocks:** —

## Scope

Migrate the five `.env`-sourced knobs out of `Settings` and into `PreferencesContext`-driven analysis. Wire the units formatter into the report renderers. Update affected tests.

## Files modified

### Core config

- `runos/config.py`:
  - **DELETE** the five `Field` declarations: `target_kcal_default` (lines 136-144), `threshold_pace_s_per_km` (161), `max_hr` (169), `resting_hr` (173), `threshold_hr` (177).
  - **ADD** `preferences_path` derived property next to `food_path` (~line 244):
    ```python
    @property
    def preferences_path(self) -> Path:
        """Path to the user-maintained preferences markdown (physiology + units + nutrition + prose)."""
        return self.content_root / "preferences.md"
    ```

### Analysis plumbing

- `runos/analysis/runner.py`:
  - Add `from runos.analysis.preferences import PreferencesContext, parse_preferences` at the import block.
  - Each top-level `generate_*` function (`generate_load_trend`, `generate_recovery`, `generate_race_readiness`, `generate_correlation`, `generate_nutrition`, `generate_all`) gains an optional `prefs: PreferencesContext | None = None` parameter. Inside the function: `if prefs is None: prefs = parse_preferences(settings.preferences_path)`.
  - Where the function currently reads `settings.threshold_pace_s_per_km` / `settings.max_hr` / `settings.resting_hr` / `settings.threshold_hr`, replace with `prefs.physiology.*`. The `LoadConfig(...)` construction at ~line 217 becomes `LoadConfig(threshold_pace_s_per_km=prefs.physiology.threshold_pace_s_per_km, max_hr=prefs.physiology.max_hr, resting_hr=prefs.physiology.resting_hr, threshold_hr=prefs.physiology.threshold_hr)`.
  - Where the function currently reads `settings.target_kcal_default`, replace with `prefs.nutrition.target_kcal`.
  - `has_config` check at line ~244 — same replacement.
  - Pass `prefs.units` through to renderers that need it (see Reports section).
- `runos/cli.py`:
  - At each `analyze` subcommand top: `prefs = parse_preferences(settings.preferences_path)`; pass `prefs` into the runner call.
  - Lines 726, 799, 854: `settings.target_kcal_default` → `prefs.nutrition.target_kcal`.
- `runos/sync/daily.py`:
  - Same shape: load `prefs` once at top, replace the five `settings.*` reads.

### Reports — units wiring

- `runos/analysis/report.py`:
  - Add `from runos.units import format_distance` and (forward-ref) the `Units` type.
  - `render_load_trend` (the function that emits the `| Week | Runs | Distance (km) | ... |` table) takes a new `units: Units` parameter (positional, after the existing arguments — or kwarg with default `Units()` for back-compat — pick whichever causes the smallest diff in tests). Column header label switches: `"Distance (km)"` vs `"Distance (mi)"`. Cell value: `format_distance(week.distance_m, units, precision=1)` — but the current `WeeklyRollup.distance_km` is pre-converted to km. **DECISION**: change `WeeklyRollup` to hold `distance_m: float` (raw metres), do conversion at render time. Touch the rollup-builder in `runner.py::weekly_rollups` to keep raw metres.
- `runos/analysis/recovery.py`:
  - The recovery report doesn't currently render distances/paces in the body, so likely a NO-OP. Confirm by greppping for `km` in the recovery render path; if present, wire `units`.
- `runos/analysis/nutrition_report.py`:
  - No distance/pace rendering — pure macros + kcal. No change.
- `runos/analysis/race.py` / race-readiness report path:
  - Riegel + VDOT use km internally. Race-readiness report renders predicted finish times — likely needs to surface predicted *pace* in user units. Audit; if currently emitted in `/km`, wire through `format_pace`.

### Top-level docs + example

- `.env.example`:
  - DELETE the `## Load & analysis settings (Phase 4)` block AND the `## Nutrition goal tracking (Phase 16)` block.
  - REPLACE with a short comment trail (5-10 lines) saying:
    ```
    # ---- Personal physiology + units + nutrition target (Phase 17) -------------
    # These knobs moved to preferences.md (in RUNOS_CONTENT_DIR). The file is
    # human-edited; the parser is lenient. See preferences.md.example for the
    # template and docs/PREFERENCES.md for the contract. Moved keys:
    #   RUNOS_THRESHOLD_PACE_S_PER_KM, RUNOS_MAX_HR, RUNOS_RESTING_HR,
    #   RUNOS_THRESHOLD_HR, RUNOS_TARGET_KCAL.
    ```

### Tests — surgical edits

Any test that currently uses `monkeypatch.setenv("RUNOS_THRESHOLD_PACE_S_PER_KM", ...)` etc. needs to either (a) construct `Physiology(...)` / `Nutrition(...)` directly and feed it through a `PreferencesContext(...)`, or (b) write a `preferences.md` to the tmp content_dir.

**Candidate files to audit + edit:**

- `tests/test_load.py` — uses `LoadConfig` directly; no Settings dependency. Likely zero change unless the `LoadConfig` construction site moved.
- `tests/test_recovery.py` — `test_recovery_renderer_appends_goal_line_when_target_set` likely sets `RUNOS_TARGET_KCAL`. Switch to direct `target_kcal=2200` argument.
- `tests/test_nutrition.py` — same; replace any `setenv("RUNOS_TARGET_KCAL", ...)` with direct argument.
- `tests/test_nutrition_report.py` — `CFG = LoadConfig(...)` already direct; check for `RUNOS_TARGET_KCAL` setenv calls.
- `tests/test_correlation.py` — `LoadConfig` direct; probably no change.
- `tests/test_cli.py` (and any `test_*_cli.py`) — these exercise full CLI flow; switch the env-var setup to writing a tmp `preferences.md`. ALSO add at least one new test that ASSERTS the CLI honours preferences.md (e.g. `test_analyze_recovery_reads_target_kcal_from_preferences_md`).
- `tests/test_runner.py` (if exists) — same.
- `tests/test_config.py` (if exists) — drop tests that asserted the deleted Settings fields existed; add a test for the new `preferences_path` derived property.
- `tests/test_setup_*` (wizard tests) — if the wizard wrote `RUNOS_THRESHOLD_PACE_S_PER_KM` to `.env`, that whole step needs revisiting. Likely the wizard's "ask for threshold pace" step should now write to `preferences.md` instead of `.env`. **SUB-DECISION**: defer the wizard's preferences-writing change to a follow-up; for now, make the wizard step a no-op (skip the threshold-pace question) and document in the SUMMARY.

## Verification (executor must run before signalling complete)

```bash
uv run pytest tests/ -x --deselect tests/test_bot_transcribe.py::test_transcribe_file_real_fixture_returns_nonempty
uv run ruff check runos/ tests/
```

Both green. Write `17-03-SUMMARY.md` covering: files touched, tests changed, any wizard step deferred.

## Risks + mitigations

- **Hidden settings reads.** `grep -rn "settings\.\(target_kcal_default\|threshold_pace\|max_hr\|resting_hr\|threshold_hr\)" tempo/` MUST return empty after the wave. Run this as the last verification step.
- **Wizard breaks.** The Phase 14 wizard wrote some of these to `.env`. If a wizard test fails, the right call is to remove the wizard step (since the source-of-truth moved). Document.
- **Test fixture drift.** Many tests rely on `LoadConfig(threshold_pace_s_per_km=240.0)` direct construction — those are fine. The risk is the env-driven path which exercises Settings → runner. Audit by grepping for `setenv.*RUNOS_THRESHOLD` and `setenv.*TARGET_KCAL`.
- **Report column label change.** If `units.distance == "km"` (the default), the label stays `"Distance (km)"` — existing report-snapshot tests don't break. The label change is GATED on a non-default Units setting.
