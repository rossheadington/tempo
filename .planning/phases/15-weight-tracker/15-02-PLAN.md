---
phase: 15-weight-tracker
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - runos/analysis/recovery.py
  - runos/analysis/runner.py
  - runos/cli.py
  - tests/test_recovery.py
autonomous: true
requirements:
  - WEIGHT-04

must_haves:
  truths:
    - "`RecoveryAssessment` has new fields `weight: WeightRollup | None = None` and `weight_present: bool = False` (defaults preserve back-compat with callers that don't pass weight)"
    - "`assess_recovery_from_db(..., weight_path=...)` parses the file, rolls it up to the report's 'as of' day, and attaches the result to the (single) re-constructed RecoveryAssessment alongside heat + strength"
    - "Today-for-weight = `points[-1].day` when points exist, else `date.today()` (mirrors the existing heat + strength alignment exactly — reuse the same `today_for_tracker` variable computed once)"
    - "When `weight_path is None` (back-compat), `weight` + `weight_present` default to None/False and the renderer omits the section"
    - "Renderer emits NO `## Weight` section when `weight_present=False` OR `weight is None`"
    - "Renderer emits NO section when `weight_present=True` AND `weight.latest_entry is None` (file parsed but zero entries ever)"
    - "Renderer emits a one-line stale nudge (`_Last weigh-in N days ago — log a current reading to keep the rollup live._`) when `weight.days_since_last > 14`"
    - "Renderer emits the active rollup line (`{latest} kg today · 7d avg {a7} kg · 28d avg {a28} kg · trend {ewma} kg · {±X.X} kg vs 28d baseline`) when `weight.days_since_last <= 14`"
    - "Weight values render with ONE decimal place (`72.4 kg`, not `72.40` or `72`); the delta renders with a leading `+` or `−` (Unicode minus) and one decimal (`+0.3 kg`, `−0.5 kg`); a delta of exactly 0 renders as `±0.0 kg`"
    - "When `weight.unit_mixed=True`, the active rollup line gains a trailing ` _(mixed kg/lb in log — normalised to kg)_` suffix"
    - "The Weight section is placed AFTER the Strength & conditioning section in the rendered report (Heat → Strength → Weight ordering)"
    - "`runner.generate_recovery` and `runner.generate_all` accept `weight_path: Path | None = None` and thread it through `assess_recovery_from_db`"
    - "`runos analyze` and `runos analyze recovery` CLI subcommands pass `settings.weight_path` next to `settings.strength_path` at both call sites"
  artifacts:
    - path: "runos/analysis/recovery.py"
      provides: "RecoveryAssessment with weight fields, assess_recovery_from_db accepting weight_path, _render_weight_section helper, _fmt_weight_delta helper, render_recovery calling the new section after strength"
      contains: "weight: WeightRollup | None, weight_present: bool, def _render_weight_section, def _fmt_weight_delta, weight_path: Path | None"
    - path: "runos/analysis/runner.py"
      provides: "generate_recovery and generate_all threading weight_path"
      contains: "weight_path: Path | None"
    - path: "runos/cli.py"
      provides: "analyze + analyze recovery CLI commands passing settings.weight_path"
      contains: "weight_path=settings.weight_path"
    - path: "tests/test_recovery.py"
      provides: "5+ new tests covering the 3-state degradation rule + delta formatting + mixed-unit caveat + section ordering"
      contains: "test_recovery_renderer_omits_weight_section_when_absent, test_recovery_renderer_omits_weight_when_present_but_empty, test_recovery_renderer_emits_stale_nudge_when_last_weigh_in_over_14d, test_recovery_renderer_emits_full_rollup_when_current, test_recovery_renderer_appends_mixed_unit_caveat, test_recovery_renderer_weight_section_follows_strength, test_fmt_weight_delta_signs"
  key_links:
    - from: "runos/cli.py::analyze_recovery"
      to: "runos/analysis/runner.py::generate_recovery"
      via: "passes weight_path=settings.weight_path"
      pattern: "weight_path=settings.weight_path"
    - from: "runos/analysis/runner.py::generate_recovery"
      to: "runos/analysis/recovery.py::assess_recovery_from_db"
      via: "passes weight_path through"
      pattern: "weight_path=weight_path"
    - from: "runos/analysis/recovery.py::render_recovery"
      to: "runos/analysis/recovery.py::_render_weight_section"
      via: "called after _render_strength_section"
      pattern: "_render_weight_section"
---

<objective>
Wire the weight parser+rollup from Plan 15-01 into the recovery report. Extend `RecoveryAssessment` with `weight` / `weight_present` fields, add a `_render_weight_section` helper that follows the same 3-state degradation rule as heat and strength (absent → omit / stale → one-line nudge / current → full rollup line), and thread `weight_path` through `assess_recovery_from_db`, `runner.generate_recovery`, `runner.generate_all`, and both CLI call sites (`runos analyze`, `runos analyze recovery`).

The Weight section renders AFTER the Strength & conditioning section, so the recovery report's non-running-context cluster is `Heat → Strength → Weight` in that order.

Purpose: makes Plan 15-01's parser actually surface in the recovery report — this is the user-observable goal-backward target (WEIGHT-04).

Output:
- `runos/analysis/recovery.py` with `weight` + `weight_present` fields on `RecoveryAssessment`, `_render_weight_section`, `_fmt_weight_delta`, and `assess_recovery_from_db` accepting `weight_path`
- `runos/analysis/runner.py` with `weight_path` threaded through `generate_recovery` + `generate_all`
- `runos/cli.py` with `weight_path=settings.weight_path` passed at both `runner.*` call sites
- `tests/test_recovery.py` extended with 6+ new tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-weight-tracker/15-CONTEXT.md
@.planning/phases/15-weight-tracker/15-01-PLAN.md
@runos/analysis/recovery.py
@runos/analysis/runner.py
@runos/cli.py
@tests/test_recovery.py
@runos/analysis/weight.py
@runos/analysis/strength.py
</context>

<interfaces>
<!-- These are the exports from Plan 15-01 (`runos/analysis/weight.py`) that this plan consumes. -->
<!-- Executor should use these directly — no need to re-explore weight.py. -->

```python
# From runos/analysis/weight.py (created in Plan 15-01)

@dataclass(frozen=True, slots=True)
class WeightEntry:
    date: date
    weight: float
    unit: str           # "kg" or "lb"
    notes: str | None
    source_line: int

@dataclass(frozen=True, slots=True)
class WeightContext:
    present: bool
    entries: tuple[WeightEntry, ...]    # sorted ascending by date, latest-wins on dupes
    path: Path | None
    malformed_lines: tuple[int, ...]

@dataclass(frozen=True, slots=True)
class WeightRollup:
    latest_entry: WeightEntry | None
    latest_kg: float | None             # latest entry kg-normalised (lb * 0.453592)
    days_since_last: int | None
    avg_7d: float | None                # kg-normalised mean over (today - 7, today]
    avg_28d: float | None               # kg-normalised mean over (today - 28, today]
    ewma_trend: float | None            # EWMA alpha=0.1 over ALL entries up to today
    delta_vs_28d: float | None          # latest_kg - avg_28d
    unit_mixed: bool                    # True iff both kg and lb appeared

def parse_weight(path: Path) -> WeightContext: ...
def weight_rollup(entries: tuple[WeightEntry, ...] | list[WeightEntry], today: date) -> WeightRollup: ...
```

<!-- Existing strength + heat plumbing (from Phase 13) is the shape to mirror.
     In particular, `assess_recovery_from_db` already does a single-reconstruction
     dance to attach heat + strength; this plan extends that pattern to also attach
     weight. See `runos/analysis/recovery.py::assess_recovery_from_db` for the
     current shape. -->
</interfaces>

<preflight_notes>
**Wave gating.** This plan depends on `15-01` shipping first (the executor must be able to `from runos.analysis.weight import parse_weight, weight_rollup, WeightRollup`). Do NOT start this plan until 15-01 is committed to `main`.

**Parallel with 15-03.** This plan runs in the same wave as 15-03 (docs/example). The two plans have ZERO `files_modified` overlap — 15-03 touches only `weight.md.example`, `docs/WEIGHT.md`, and `README.md`. No coordination needed.

**Existing heat + strength plumbing is the template.** `assess_recovery_from_db` already accepts `heat_path` + `strength_path` (Phase 13 added strength). The change here is a third additive kwarg `weight_path` threaded through the same single-reconstruction pattern.
</preflight_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend RecoveryAssessment + assess_recovery_from_db with weight fields, add _render_weight_section + _fmt_weight_delta</name>
  <files>runos/analysis/recovery.py</files>
  <read_first>
    - runos/analysis/recovery.py (imports block — add `from runos.analysis.weight import WeightRollup`, `from runos.analysis.weight import parse_weight as _parse_weight`, `from runos.analysis.weight import weight_rollup as _weight_rollup`, mirroring the existing heat + strength imports)
    - runos/analysis/recovery.py (`RecoveryAssessment` dataclass — `weight` + `weight_present` go immediately after `strength` + `strength_present`)
    - runos/analysis/recovery.py (`assess_recovery_from_db` — Phase 13 refactored this to do a single reconstruction with both heat + strength; add the weight block to the same single reconstruction)
    - runos/analysis/recovery.py (`_render_strength_section` — `_render_weight_section` mirrors it; `_fmt_weight_delta` is new, sits near `_fmt_tonnage`)
    - runos/analysis/recovery.py (`render_recovery` — call `_render_weight_section(out, a.weight, a.weight_present)` immediately AFTER the existing `_render_strength_section(...)` call)
    - .planning/phases/15-weight-tracker/15-CONTEXT.md `### Recovery-report integration (LOCKED)` section (lines 112-125)
  </read_first>
  <behavior>
    Imports added (top of file, alongside heat + strength imports):
    - `from runos.analysis.weight import WeightRollup`
    - `from runos.analysis.weight import parse_weight as _parse_weight`
    - `from runos.analysis.weight import weight_rollup as _weight_rollup`

    `RecoveryAssessment` gains two new fields right after `strength_present`:
    - `weight: WeightRollup | None = None`
    - `weight_present: bool = False`

    `_fmt_weight_delta(delta_kg: float) -> str` (new helper, near `_fmt_tonnage`):
    - `delta_kg > 0`: `f"+{delta_kg:.1f} kg"` (e.g. `"+0.3 kg"`)
    - `delta_kg < 0`: `f"−2{abs(delta_kg):.1f} kg"` — NO, simpler: `f"−{abs(delta_kg):.1f} kg"` using the actual Unicode minus character `−` (visually distinct from hyphen, matches the typography used in CONTEXT line 123 example `−0.5 kg`).
    - `delta_kg == 0.0` (or `abs(delta_kg) < 0.05` after rounding): `"±0.0 kg"` using the `±` character (U+00B1) — covers the round-to-zero edge case so a 0.04 kg shift doesn't render as `+0.0 kg` (ambiguous sign).

    `_render_weight_section(out: list[str], weight: WeightRollup | None, weight_present: bool) -> None` (new, mirrors `_render_strength_section`):
    - `weight_present is False` OR `weight is None` → return (omit section entirely).
    - `weight_present is True` but `weight.latest_entry is None` → return (file parsed, zero entries ever — omit).
    - `weight_present is True`, `weight.latest_entry is not None`, `weight.days_since_last > 14`:
      - Append `"## Weight\n"` then a single-line stale nudge: `f"_Last weigh-in {weight.days_since_last} days ago — log a current reading to keep the rollup live._"`. Append a trailing `""`.
    - Current state (`weight.days_since_last <= 14`):
      - Append `"## Weight\n"`.
      - Build the active rollup line:
        ```
        {latest_kg:.1f} kg today · 7d avg {avg_7d:.1f} kg · 28d avg {avg_28d:.1f} kg · trend {ewma_trend:.1f} kg · {delta_fmt} vs 28d baseline
        ```
        where `delta_fmt = _fmt_weight_delta(weight.delta_vs_28d)`.
      - If `weight.unit_mixed is True`, append ` _(mixed kg/lb in log — normalised to kg)_` to the line.
      - Defensive: if `avg_7d` or `avg_28d` or `ewma_trend` or `delta_vs_28d` is None (shouldn't happen in current state since `days_since_last <= 14` implies at least one entry within 28 days, but be defensive), substitute the string `"n/a"` for that segment rather than crashing.
      - Append a trailing `""`.

    `assess_recovery_from_db` signature extended to accept `weight_path: Path | None = None` as the LAST keyword-only argument (after `strength_path`):
    - The Phase 13 refactor already collapsed heat + strength into a single reconstruction with one `today_for_tracker` variable. Add the weight block to the same body — parse + roll up + capture `weight_rollup_obj` and `weight_present`, then include both in the final `RecoveryAssessment(...)` construction.
    - The early-exit `if heat_path is None and strength_path is None` (if it still exists) must be updated to also check `weight_path is None` — otherwise weight-only callers would never reach the reconstruction.

    `render_recovery`: immediately AFTER the existing `_render_strength_section(out, a.strength, a.strength_present)` line, add a single new line: `_render_weight_section(out, a.weight, a.weight_present)`. Same indentation, same shape.
  </behavior>
  <action>
    Implementation order (TDD-friendly):

    1. **Add the imports** at the top of `recovery.py`, alongside the existing heat + strength imports.

    2. **Add the dataclass fields** to `RecoveryAssessment` immediately after `strength_present: bool = False`: `weight: WeightRollup | None = None` then `weight_present: bool = False`.

    3. **Add `_fmt_weight_delta`** as a new module-level function near `_fmt_tonnage`. Use the Unicode minus `−` for negative deltas and `±` for the zero case. One short docstring.

    4. **Add `_render_weight_section`** as a new module-level function immediately AFTER `_render_strength_section`. Match the docstring tone and the 3-state structure. Be defensive about None-valued rollup fields when in current state.

    5. **Extend `assess_recovery_from_db`** to add the `weight_path` kwarg + the weight block. The function already does a single reconstruction with heat + strength after Phase 13; add weight to the same pattern. Recommended shape:

       ```python
       def assess_recovery_from_db(
           conn,
           *,
           points,
           guardrail,
           window=baselines.DEFAULT_WINDOW,
           min_points=baselines.MIN_POINTS,
           heat_path: Path | None = None,
           strength_path: Path | None = None,
           weight_path: Path | None = None,
       ) -> RecoveryAssessment:
           latest = baselines.latest_baselines(conn, window=window, min_points=min_points)
           day = points[-1].day if points else None
           assessment = assess_recovery(
               day=day, points=points, guardrail=guardrail, latest_baselines=latest
           )

           if heat_path is None and strength_path is None and weight_path is None:
               return assessment

           # Align all tracker windows with the recovery report's "as of" day.
           if points:
               try:
                   today_for_tracker = date.fromisoformat(points[-1].day)
               except ValueError:
                   today_for_tracker = date.today()
           else:
               today_for_tracker = date.today()

           heat_rollup_obj: HeatRollup | None = None
           heat_present = False
           if heat_path is not None:
               heat_ctx = _parse_heat(heat_path)
               heat_rollup_obj = _heat_rollup(heat_ctx.sessions, today_for_tracker)
               heat_present = heat_ctx.present

           strength_rollup_obj: StrengthRollup | None = None
           strength_present = False
           if strength_path is not None:
               strength_ctx = _parse_strength(strength_path)
               strength_rollup_obj = _strength_rollup(strength_ctx.sessions, today_for_tracker)
               strength_present = strength_ctx.present

           weight_rollup_obj: WeightRollup | None = None
           weight_present = False
           if weight_path is not None:
               weight_ctx = _parse_weight(weight_path)
               weight_rollup_obj = _weight_rollup(weight_ctx.entries, today_for_tracker)
               weight_present = weight_ctx.present

           return RecoveryAssessment(
               day=assessment.day,
               status=assessment.status,
               rising_load=assessment.rising_load,
               load_reasons=assessment.load_reasons,
               signals=assessment.signals,
               messages=assessment.messages,
               heat=heat_rollup_obj,
               heat_present=heat_present,
               strength=strength_rollup_obj,
               strength_present=strength_present,
               weight=weight_rollup_obj,
               weight_present=weight_present,
           )
       ```

       (The exact field list in the final `RecoveryAssessment(...)` constructor depends on what the current frozen dataclass has — copy it verbatim from the existing code, just add the two new `weight=` / `weight_present=` kwargs.)

    6. **Update the docstring** of `assess_recovery_from_db` to mention `weight_path` alongside `heat_path` + `strength_path` (one short paragraph addition). Same tone as the existing strength docstring.

    7. **Wire the renderer**: add `_render_weight_section(out, a.weight, a.weight_present)` immediately AFTER `_render_strength_section(out, a.strength, a.strength_present)` in `render_recovery`.

    8. **Do not change** the standalone `assess_recovery` function — weight integration only flows through `_from_db`, mirroring how heat + strength work.
  </action>
  <verify>
    <automated>uv run pytest tests/test_recovery.py -x -v</automated>
    All existing tests still pass; new tests added in Task 4 are not yet present at this point — that's fine, this verifies no regression in the existing heat + strength + recovery render tests.
    Also: `uv run ruff check runos/analysis/recovery.py` passes.
    Also smoke: `uv run python -c "from runos.analysis.recovery import RecoveryAssessment, _render_weight_section, _fmt_weight_delta; r = RecoveryAssessment(day='2026-05-28', status='ok', rising_load=False, load_reasons=[], signals=[]); assert r.weight is None and r.weight_present is False; print(_fmt_weight_delta(0.3), _fmt_weight_delta(-0.5), _fmt_weight_delta(0.0))"` → prints `+0.3 kg −0.5 kg ±0.0 kg`.
  </verify>
  <done>
    `RecoveryAssessment` carries `weight` + `weight_present` (defaults preserve back-compat); `assess_recovery_from_db` accepts `weight_path` and threads it through alongside heat + strength (single reconstruction); `_render_weight_section` and `_fmt_weight_delta` exist; `render_recovery` calls the new section after strength; ruff clean; existing recovery tests still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Thread weight_path through runner.generate_recovery and runner.generate_all</name>
  <files>runos/analysis/runner.py</files>
  <read_first>
    - runos/analysis/runner.py (`generate_recovery` signature + body — find the existing `strength_path: Path | None = None` kwarg added in Phase 13; `weight_path` goes immediately after it)
    - runos/analysis/runner.py (`generate_all` signature + body — same pattern; `weight_path` threaded to the inner `generate_recovery` call)
  </read_first>
  <action>
    1. **`generate_recovery`**: add `weight_path: Path | None = None` as a new keyword-only argument immediately after `strength_path: Path | None = None` in the signature. Default = None for back-compat.
       - In the body, pass `weight_path=weight_path` into the `recovery_mod.assess_recovery_from_db(...)` call alongside `heat_path=heat_path` and `strength_path=strength_path`.
       - Update the docstring (one short addition): "If `weight_path` is provided, the parsed rollup is rendered into a `## Weight` section using the same 3-state degradation rule as heat and strength."

    2. **`generate_all`**: add `weight_path: Path | None = None` to the signature immediately after `strength_path: Path | None = None`. In the body, pass `weight_path=weight_path` into the inner `generate_recovery(...)` call.

    3. No other runner changes — only `generate_recovery` reads weight data, so the threading stops at those two functions.
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q -k "runner or recovery or analyze"</automated>
    Also: `uv run ruff check runos/analysis/runner.py` passes.
    Also smoke: `uv run python -c "import inspect; from runos.analysis.runner import generate_recovery, generate_all; assert 'weight_path' in inspect.signature(generate_recovery).parameters; assert 'weight_path' in inspect.signature(generate_all).parameters; print('OK')"`.
  </verify>
  <done>
    Both `generate_recovery` and `generate_all` accept `weight_path: Path | None = None` (defaults preserve back-compat) and thread it through to `assess_recovery_from_db`; ruff clean; no test regressions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pass settings.weight_path at both CLI call sites in runos/cli.py</name>
  <files>runos/cli.py</files>
  <read_first>
    - runos/cli.py (the `analyze` top-level command — the `runner.generate_all(...)` call with `strength_path=settings.strength_path` from Phase 13; `weight_path=settings.weight_path` goes immediately after)
    - runos/cli.py (the `analyze recovery` subcommand — the `runner.generate_recovery(...)` call with `strength_path=settings.strength_path` from Phase 13; same additive line)
  </read_first>
  <action>
    1. **`runos analyze`**: in the `runner.generate_all(...)` call, add `weight_path=settings.weight_path,` on the line immediately after `strength_path=settings.strength_path,`. Same indentation, same comma-trailing style. **One single line addition.**

    2. **`runos analyze recovery`**: in the `runner.generate_recovery(...)` call, add `weight_path=settings.weight_path,` on the line immediately after `strength_path=settings.strength_path,`. **One single line addition.**

    3. No other CLI changes. Do NOT add a new `runos weight` subcommand (out of scope per CONTEXT lines 26-27 — that's deferred).
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q -k "cli"</automated>
    Also: `uv run ruff check runos/cli.py` passes.
    Also smoke: `uv run python -c "import subprocess; r = subprocess.run(['uv', 'run', 'tempo', 'analyze', '--help'], capture_output=True, text=True); assert r.returncode == 0; assert 'recovery' in r.stdout; print('OK')"`.
    Also: `grep -c "weight_path=settings.weight_path" runos/cli.py` returns ≥ 2 (one per call site).
  </verify>
  <done>
    Both CLI call sites (`runos analyze` and `runos analyze recovery`) pass `weight_path=settings.weight_path`; ruff clean; no test regressions; CLI still imports + runs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Extend tests/test_recovery.py with new tests covering the weight integration</name>
  <files>tests/test_recovery.py</files>
  <read_first>
    - tests/test_recovery.py (find the Phase 13 strength section block — the comment divider `# ---- Strength & conditioning section ----` is the anchor; the new weight section tests go immediately below it under a `# ---- Weight section ----` divider)
    - tests/test_recovery.py (find the `_ok_assessment` builder helper extended in Phase 13 to accept `strength` / `strength_present` kwargs — extend it again to accept `weight` / `weight_present` defaulting to None / False)
  </read_first>
  <behavior>
    New tests added to `tests/test_recovery.py` (grouped under a `# ---- Weight section ----` divider, immediately after the strength section block):

    1. `test_recovery_renderer_omits_weight_section_when_absent`:
       - Build an assessment with `weight=None, weight_present=False`.
       - Render it.
       - Assert `"## Weight"` is NOT in the output.

    2. `test_recovery_renderer_omits_weight_when_present_but_empty`:
       - Build a `WeightRollup` with `latest_entry=None` and `weight_present=True`.
       - Render.
       - Assert `"## Weight"` is NOT in the output.

    3. `test_recovery_renderer_emits_stale_nudge_when_last_weigh_in_over_14d`:
       - Build a `WeightRollup` with `latest_entry=<some entry>`, `days_since_last=21`, `latest_kg=72.4`, all the avg/trend/delta fields populated. Set `weight_present=True`.
       - Render.
       - Assert `"## Weight"` IS in the output.
       - Assert the stale nudge fragment is present: `"Last weigh-in 21 days ago"` AND `"log a current reading"`.
       - Assert the active rollup markers (`"7d avg"`, `"trend"`, `"vs 28d baseline"`) are NOT present (the stale nudge is the ONLY weight line).

    4. `test_recovery_renderer_emits_full_rollup_when_current`:
       - Build a `WeightRollup` with `days_since_last=1`, `latest_kg=72.4`, `avg_7d=72.6`, `avg_28d=72.9`, `ewma_trend=72.8`, `delta_vs_28d=-0.5`, `unit_mixed=False`. Set `weight_present=True`.
       - Render.
       - Assert `"## Weight"` IS in the output.
       - Assert the active rollup line contains: `"72.4 kg today"`, `"7d avg 72.6 kg"`, `"28d avg 72.9 kg"`, `"trend 72.8 kg"`, `"−0.5 kg vs 28d baseline"` (Unicode minus, ONE decimal).
       - Assert the mixed-unit caveat is NOT present.

    5. `test_recovery_renderer_appends_mixed_unit_caveat`:
       - Build a `WeightRollup` identical to test 4 but with `unit_mixed=True`.
       - Render.
       - Assert `"## Weight"` IS in the output AND the trailing caveat `"_(mixed kg/lb in log — normalised to kg)_"` is appended to the active rollup line.

    6. `test_recovery_renderer_weight_section_follows_strength`:
       - Build an assessment with BOTH an active strength rollup AND a current weight rollup.
       - Render.
       - Assert both `"## Strength & conditioning"` and `"## Weight"` are present.
       - Assert the index of `"## Strength & conditioning"` is LESS than the index of `"## Weight"` in the rendered text (weight comes after strength).

    7. `test_fmt_weight_delta_signs`:
       - Import `_fmt_weight_delta` from `runos.analysis.recovery`.
       - Assert `_fmt_weight_delta(0.3) == "+0.3 kg"`.
       - Assert `_fmt_weight_delta(-0.5) == "−0.5 kg"` (Unicode minus `−`).
       - Assert `_fmt_weight_delta(0.0) == "±0.0 kg"` (Unicode plus-minus `±`).
       - Assert `_fmt_weight_delta(1.25) == "+1.2 kg"` OR `"+1.3 kg"` (one-decimal rounding; verify whichever Python's default banker's rounding produces — either is acceptable as long as the test matches the implementation).

    If the existing `_ok_assessment` helper doesn't accept `weight` / `weight_present` kwargs, extend it to do so (defaults `weight=None, weight_present=False`) so the new tests can call it the same way the heat + strength tests do.
  </behavior>
  <action>
    1. Open `tests/test_recovery.py`. Find the `_ok_assessment` helper (extended in Phase 13). Extend its signature to accept `weight: object = None, weight_present: bool = False` (mirroring the existing `strength` / `strength_present` kwargs) and pass them through to `RecoveryAssessment(...)`.

    2. Find the strength section tests + the `# ---- Strength & conditioning section ----` divider. Below the last strength test, add a divider comment: `# ---- Weight section ----`.

    3. Write the 7 new tests per the <behavior> spec. Import `WeightRollup` and `WeightEntry` from `runos.analysis.weight` at the test-function level (mirror how the strength tests import `StrengthRollup`). For `WeightRollup` fields that need a populated `latest_entry`, build a small `WeightEntry` inline with sensible values.

    4. The `test_fmt_weight_delta_signs` test imports `_fmt_weight_delta` from `runos.analysis.recovery` (the helper is module-private by convention but importable for tests, mirroring how `_fmt_tonnage` is tested).
  </action>
  <verify>
    <automated>uv run pytest tests/test_recovery.py -x -v</automated>
    All existing recovery tests still pass; the 7 new tests pass.
    Also: `uv run pytest tests/ -x --deselect tests/test_bot_transcribe.py::test_transcribe_file_real_fixture_returns_nonempty` — full suite green.
    Also: `uv run ruff check tests/test_recovery.py` passes.
    Also: `grep -c "^def test_recovery_renderer_omits_weight_section_when_absent\|^def test_recovery_renderer_omits_weight_when_present_but_empty\|^def test_recovery_renderer_emits_stale_nudge_when_last_weigh_in_over_14d\|^def test_recovery_renderer_emits_full_rollup_when_current\|^def test_recovery_renderer_appends_mixed_unit_caveat\|^def test_recovery_renderer_weight_section_follows_strength\|^def test_fmt_weight_delta_signs" tests/test_recovery.py` returns 7.
  </verify>
  <done>
    7 new weight tests added; all pass; full suite green; ruff clean; the 3-state degradation rule + delta formatting + mixed-unit caveat + section ordering are verified.
  </done>
</task>

</tasks>

<verification>
**Plan-level checks** (run after all 4 tasks complete):

- `uv run pytest tests/ -x --deselect tests/test_bot_transcribe.py::test_transcribe_file_real_fixture_returns_nonempty` → full suite green
- `uv run ruff check runos/analysis/recovery.py runos/analysis/runner.py runos/cli.py tests/test_recovery.py` → clean
- Smoke (end-to-end against a fake): `uv run python -c "from runos.analysis.recovery import _render_weight_section, _fmt_weight_delta; from runos.analysis.weight import WeightRollup, WeightEntry; from datetime import date; e = WeightEntry(date=date(2026,5,27), weight=72.4, unit='kg', notes=None, source_line=1); r = WeightRollup(latest_entry=e, latest_kg=72.4, days_since_last=1, avg_7d=72.6, avg_28d=72.9, ewma_trend=72.8, delta_vs_28d=-0.5, unit_mixed=False); out = []; _render_weight_section(out, r, True); print('\n'.join(out))"` — emits an active rollup line with `72.4 kg today` and `−0.5 kg vs 28d baseline`.

**Goal-backward check** (proves WEIGHT-04):
- A live `runos analyze recovery` run against a DB + a `weight.md` file containing a current weigh-in must produce a `recovery-YYYY-MM-DD.md` report containing a `## Weight` section with the latest reading, 7d avg, 28d avg, EWMA trend, and delta vs 28d baseline. Verifiable post-merge by the maintainer; not part of automated CI.
</verification>

<success_criteria>
- `RecoveryAssessment` has `weight: WeightRollup | None = None` and `weight_present: bool = False`, defaults preserve back-compat
- `assess_recovery_from_db` accepts `weight_path: Path | None = None`, parses + rolls + attaches via the same single reconstruction that already carries heat + strength
- `_render_weight_section` honours the 3-state rule (absent → omit / present-but-empty → omit / stale → one-line nudge / current → full rollup line)
- `_fmt_weight_delta` returns `"+0.3 kg"` / `"−0.5 kg"` / `"±0.0 kg"` per the locked formatting (Unicode minus + plus-minus)
- The Weight section renders AFTER the Strength section in `render_recovery` (final order: Heat → Strength → Weight)
- `runner.generate_recovery` and `runner.generate_all` accept + thread `weight_path` (default None)
- Both CLI sites (`runos analyze`, `runos analyze recovery`) pass `settings.weight_path`
- 7 new tests in `tests/test_recovery.py` all pass; full suite green; ruff clean
- No bot/journal/migration/transforms files touched
</success_criteria>

<output>
Create `.planning/phases/15-weight-tracker/15-02-SUMMARY.md` when done. Summary must report:
- Confirmation that the recovery report renders the weight section in the expected order (after strength, before any nutrition section reserved for Phase 16)
- Test count delta in `tests/test_recovery.py` (target: +7)
- Confirmation that `assess_recovery_from_db`'s single-reconstruction now carries all three trackers (heat + strength + weight)
- Commit hash for the atomic commit
</output>

<commit_message>
```
feat(15-02): surface weight rollup in the recovery report

Wires the Plan 15-01 weight parser into the recovery report so the
owner's `weight.md` log appears alongside heat + strength in the
daily report.

- RecoveryAssessment gains `weight: WeightRollup | None` and
  `weight_present: bool` fields (defaults preserve back-compat)
- assess_recovery_from_db accepts an optional `weight_path` and
  uses the same single-reconstruction pattern that already carries
  heat + strength
- New `_render_weight_section` follows the same 3-state degradation
  rule (absent / stale-over-14d / current)
- New `_fmt_weight_delta` helper renders `+0.3 kg` / `−0.5 kg` /
  `±0.0 kg` with Unicode minus + plus-minus glyphs
- When `unit_mixed=True`, the active rollup line gains a trailing
  `_(mixed kg/lb in log — normalised to kg)_` caveat
- Section is placed AFTER strength so the recovery report's
  non-running-context cluster reads Heat → Strength → Weight
- runner.generate_recovery + generate_all thread weight_path through
- CLI: `runos analyze` and `runos analyze recovery` both pass
  settings.weight_path next to settings.strength_path
- 7 new tests in tests/test_recovery.py covering the 3 states +
  delta formatting + mixed-unit caveat + section ordering

WEIGHT-04 addressed.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
</commit_message>
</output>
</content>
</invoke>