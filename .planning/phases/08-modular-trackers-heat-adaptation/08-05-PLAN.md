---
phase: 08-modular-trackers-heat-adaptation
plan: 05
type: execute
wave: 3
depends_on:
  - 08-04
files_modified:
  - runos/analysis/context.py
  - runos/analysis/__init__.py
  - runos/analysis/runner.py
  - runos/analysis/report.py
  - runos/config.py
  - runos/cli.py
  - runos/sync/daily.py
  - .env.example
  - README.md
  - tests/test_context.py
  - tests/test_config.py
  - tests/test_analysis_reports.py
  - tests/test_analyze_cli.py
  - plan.md.example
autonomous: true
requirements:
  - TRACK-06

must_haves:
  truths:
    - "After this plan, `from runos.analysis.context import parse_plan` raises ImportError"
    - "After this plan, `settings.plan_path` raises AttributeError (the property is removed)"
    - "After this plan, plan.md.example does not exist at repo root"
    - "After this plan, the race-readiness report renders cleanly with no '## Plan context' section and no errors"
    - "After this plan, `grep -rn 'plan_path\\|parse_plan\\|PlanContext\\|plan.md' runos/ tests/` returns ZERO non-comment matches (only the example.md filename mention, if any, in test fixtures)"
    - "runos/analysis/context.py is deleted (now zero need for the shim — all callers migrated)"
    - "Full test suite passes"
  artifacts:
    - path: "runos/analysis/races.py"
      provides: "the canonical races parser (unchanged from Plan A)"
      contains: "parse_races, Race, RacesContext"
  key_links:
    - from: "all callers of runos.analysis.context"
      to: "runos.analysis.races (parse_races) directly"
      via: "imports rewritten — 8 import sites per RESEARCH.md section 7"
      pattern: "from runos.analysis.races import"
    - from: "deleted plan.md.example"
      to: "(nothing — pure removal)"
      via: "git rm"
      pattern: "(absence)"
---

<objective>
Complete the plan.md retirement (TRACK-06) and finish the context.py → races.py rename started in Plan A. This is pure deletion + import-site migration. After this plan:

- `runos/analysis/context.py` is deleted entirely (the transition shim from Plan A is no longer needed)
- All 8 import sites from RESEARCH.md section 7 are rewritten to import from `runos.analysis.races` directly
- `parse_plan`, `PlanContext`, `_PLAN_FIELD_RE` are gone from the codebase
- `settings.plan_path` property is removed
- `plan.md.example` is deleted
- Plan/plan-context references removed from runner.py, report.py, cli.py, sync/daily.py, .env.example, README.md
- Plan tests in test_context.py + plan_path assertions in test_config.py are removed; test_context.py file itself is deleted (its remaining content was the shim assertions from Plan A — those are also irrelevant once the shim is gone)
- Race-readiness report's `plan_ctx` parameter is removed from render_race_readiness

This is the largest "blast radius" plan but it's purely mechanical: every site is enumerated in RESEARCH.md section 4 (including the items PATTERNS.md missed). Run the test suite continuously to catch any callers that surface during execution.

Purpose: Hard cut. Single-user repo, no compatibility shim needed. plan.md is gone after this plan ships.

Output:
- All plan.md / plan_path / PlanContext / parse_plan code references removed
- Race-readiness report renders cleanly without the plan block
- Full test suite green
- README + .env.example reflect the new tracker landscape (just races + heat)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-modular-trackers-heat-adaptation/08-CONTEXT.md
@.planning/phases/08-modular-trackers-heat-adaptation/08-RESEARCH.md
@.planning/phases/08-modular-trackers-heat-adaptation/08-PATTERNS.md
@.planning/phases/08-modular-trackers-heat-adaptation/08-04-PLAN.md
@runos/analysis/context.py
@runos/analysis/runner.py
@runos/analysis/report.py
@runos/config.py
@runos/cli.py
@runos/sync/daily.py
@.env.example
@README.md
@tests/test_context.py
@tests/test_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the 8 import sites to point at runos.analysis.races directly</name>
  <files>runos/analysis/runner.py, runos/analysis/report.py, runos/sync/daily.py, runos/analysis/__init__.py, tests/test_noteworthy.py</files>
  <read_first>
    - .planning/phases/08-modular-trackers-heat-adaptation/08-RESEARCH.md section 7 lines 401-432 (the verbatim 8 import-site list with file:line references)
    - runos/analysis/runner.py (lines 23, 29 — `from runos.analysis import context as ctx` + `from runos.analysis.context import Race`)
    - runos/analysis/report.py (line 20 — `from runos.analysis.context import PlanContext, Race, RacesContext`)
    - runos/sync/daily.py (line 34 — `from runos.analysis import context as ctx`)
    - runos/analysis/__init__.py (line 16 — docstring mentions `runos.analysis.context`)
    - tests/test_noteworthy.py (lines 220, 233 — `from runos.analysis.context import Race, RacesContext`)
  </read_first>
  <action>
    Per RESEARCH.md section 7's enumerated 8 sites:
    1. runos/analysis/runner.py:23 `from runos.analysis import context as ctx` → `from runos.analysis import races as ctx` (keeps `ctx.parse_races` call sites in the same file untouched).
    2. runos/analysis/runner.py:29 `from runos.analysis.context import Race` → `from runos.analysis.races import Race`.
    3. runos/analysis/report.py:20 `from runos.analysis.context import PlanContext, Race, RacesContext` → `from runos.analysis.races import Race, RacesContext` (drop PlanContext — Task 4 removes its usage).
    4. runos/sync/daily.py:34 `from runos.analysis import context as ctx` → `from runos.analysis import races as ctx`.
    5. runos/analysis/__init__.py:16 docstring `* :mod:`runos.analysis.context` -- parse `races.md` / `plan.md`.` → `* :mod:`runos.analysis.races` -- parse `races.md`.` Also confirm the heat bullet added in Plan B is present; add it if not.
    6. tests/test_noteworthy.py:220 `from runos.analysis.context import Race, RacesContext` → `from runos.analysis.races import Race, RacesContext`.
    7. tests/test_noteworthy.py:233 `from runos.analysis.context import RacesContext` → `from runos.analysis.races import RacesContext`.
    8. tests/test_context.py:14 — this whole file is deleted in Task 5, so this import site needs no edit (file deletion supersedes).

    After all rewrites: `grep -rn "from runos.analysis.context\|from runos.analysis import context" runos/ tests/` should return ZERO matches.
  </action>
  <verify>
    <automated>uv run pytest tests/test_noteworthy.py -x</automated>
    test_noteworthy still passes after the import rewrite.
    Also: `grep -rn "from runos.analysis.context\|from runos.analysis import context" runos/ tests/` returns empty.
  </verify>
  <acceptance_criteria>
    - `grep -rn "from runos.analysis.context\|from runos.analysis import context" runos/ tests/` returns ZERO matches
    - `grep -c "from runos.analysis import races as ctx\|from runos.analysis.races import" runos/analysis/runner.py runos/analysis/report.py runos/sync/daily.py tests/test_noteworthy.py` returns ≥ 5
    - `uv run pytest tests/test_noteworthy.py -x` exits 0
  </acceptance_criteria>
  <done>
    All 8 import sites migrated; no more references to runos.analysis.context anywhere; tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete runos/analysis/context.py and tests/test_context.py</name>
  <files>runos/analysis/context.py, tests/test_context.py</files>
  <read_first>
    - runos/analysis/context.py (the shim from Plan A — confirm it can be safely deleted; the shim still re-exports parse_plan + PlanContext, but Task 1 above migrated all races import sites and Tasks 3-4 below remove the plan callers)
    - tests/test_context.py (the residual file from Plan A — should contain a few shim-assertion tests + the original plan-parser tests; all of it is obsolete after this plan)
  </read_first>
  <action>
    1. Delete runos/analysis/context.py entirely (use `git rm runos/analysis/context.py` — or delete the file via Write replacement is not appropriate; use Bash `rm runos/analysis/context.py` if that's the only deletion mechanism available in this environment).
    2. Delete tests/test_context.py entirely (same — `rm tests/test_context.py`).
    3. After deletion, run a quick grep to confirm no remaining code imports from `runos.analysis.context`: `grep -rn "runos.analysis.context\|from runos.analysis import context" runos/ tests/` must return ZERO matches. If any match remains, Task 1 missed a site; track it down and fix before completing this task.
  </action>
  <verify>
    <automated>test ! -e runos/analysis/context.py && test ! -e tests/test_context.py && echo "DELETED OK"</automated>
    Also: `grep -rn "runos.analysis.context\|from runos.analysis import context" runos/ tests/` returns no output.
    Also: `uv run pytest tests/ -x --collect-only 2>&1 | grep -c "test_context"` returns 0.
  </verify>
  <acceptance_criteria>
    - `runos/analysis/context.py` does not exist (`test ! -e ...` exits 0)
    - `tests/test_context.py` does not exist (`test ! -e ...` exits 0)
    - `grep -rn "runos.analysis.context\|from runos.analysis import context" runos/ tests/` returns no matches
    - `uv run pytest tests/ -x --collect-only` does not list any test_context.py tests
  </acceptance_criteria>
  <done>
    The transitional shim and its tests are gone; only races.py + test_races.py remain as the canonical home for race parsing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove plan_path property + plan_path test assertions from config layer</name>
  <files>runos/config.py, tests/test_config.py</files>
  <read_first>
    - runos/config.py (lines 146-149, the plan_path property — to be deleted)
    - tests/test_config.py (lines 38, 46 per RESEARCH.md section 4 — the two `settings.plan_path == ...` assertions — to be deleted; heat_path assertion added in Plan B stays)
    - .planning/phases/08-modular-trackers-heat-adaptation/08-PATTERNS.md section "MOD: runos/config.py (paths)" lines 537-568
    - .planning/phases/08-modular-trackers-heat-adaptation/08-RESEARCH.md section 4 (lists tests/test_config.py:38,46 as a MISSED-by-PATTERNS.md item)
  </read_first>
  <action>
    1. runos/config.py: delete the `plan_path` property (lines 146-149) entirely. Leave races_path and heat_path untouched.
    2. tests/test_config.py: delete the two `settings.plan_path == ...` assertions per RESEARCH.md section 4. Keep the heat_path assertion (added in Plan B Task 3) and the races_path assertion (existing).
    3. (Optional but recommended) Add a defensive test `test_no_plan_path_attribute_on_settings` that asserts `not hasattr(settings, 'plan_path')` — catches an incomplete retirement (RESEARCH.md section 6 P1 test).
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def plan_path\|plan_path" runos/config.py` returns 0
    - `grep -c "settings.plan_path\|plan_path" tests/test_config.py` is 0 OR is 1 (only the optional defensive test `not hasattr`)
    - `grep -c "settings.heat_path" tests/test_config.py` returns ≥ 1 (heat_path assertion from Plan B preserved)
    - `uv run pytest tests/test_config.py -x` exits 0
    - `uv run python -c "from runos.config import Settings; s = Settings(); assert not hasattr(s, 'plan_path'), 'plan_path still exists'; print('OK')"` exits 0
  </acceptance_criteria>
  <done>
    plan_path is gone from config + tests; heat_path is unchanged; defensive test pins the retirement.
  </done>
</task>

<task type="auto">
  <name>Task 4: Remove plan_path / plan_ctx from runner.py, report.py, cli.py, sync/daily.py</name>
  <files>runos/analysis/runner.py, runos/analysis/report.py, runos/cli.py, runos/sync/daily.py</files>
  <read_first>
    - runos/analysis/runner.py (lines 258-286 generate_race_readiness — remove plan_path param + parse_plan call + plan_ctx pass-through; lines 353-386 generate_all — remove plan_path param)
    - runos/analysis/report.py (lines 188-198 render_race_readiness signature — remove plan_ctx param; lines 219-223 the plan-rendering block — delete)
    - runos/cli.py (the analyze command sites RESEARCH.md section 4 says lines 449, 493 — two `plan_path=settings.plan_path,` kwarg sites to remove)
    - runos/sync/daily.py (line ~110 per RESEARCH.md — one `plan_path=settings.plan_path,` kwarg site to remove)
    - .planning/phases/08-modular-trackers-heat-adaptation/08-PATTERNS.md section "MOD: runos/analysis/report.py" + "MOD: runos/analysis/runner.py" (lines 410-444 + 447-533)
  </read_first>
  <action>
    1. runos/analysis/runner.py:
       - generate_race_readiness signature: remove `plan_path: Path,` keyword param (was line 263).
       - generate_race_readiness body: remove `plan_ctx = ctx.parse_plan(plan_path)` (was line 270).
       - generate_race_readiness body: remove `plan_ctx=plan_ctx,` kwarg from the `report_mod.render_race_readiness(...)` call (was line 281).
       - generate_all signature: remove `plan_path: Path,` keyword param (was line 358).
       - generate_all body: remove `plan_path=plan_path,` kwarg from the internal `generate_race_readiness(...)` call (was line 376).

    2. runos/analysis/report.py:
       - render_race_readiness signature: remove `plan_ctx: PlanContext,` keyword param (was line 194).
       - render_race_readiness body: remove the entire plan-rendering block (was lines 219-223):
         ```
         if plan_ctx.present and plan_ctx.fields:
             out.append("## Plan context\n")
             for key, val in plan_ctx.fields.items():
                 out.append(f"- **{key}**: {val}")
             out.append("")
         ```
       - report.py imports (line 20 after Task 1): confirm PlanContext is no longer imported.

    3. runos/cli.py: search for `plan_path=` and remove both kwarg sites (RESEARCH.md says lines 449 + 493 — confirm by grep, the exact lines may have shifted after Plan D's edits).

    4. runos/sync/daily.py: search for `plan_path=` and remove the single kwarg site (RESEARCH.md says line 110).

    5. After this task: `grep -rn "plan_ctx\|plan_path" runos/ tests/` should return ZERO matches (excluding plan_path that may appear in old test commit history — only LIVE source matters). The only remaining `plan.md` mentions in the codebase will be in .env.example and README.md, addressed in Task 5.
  </action>
  <verify>
    <automated>uv run pytest tests/ -x</automated>
    Full test suite passes — confirms no caller is still passing plan_path / plan_ctx.
    Also: `grep -rn "plan_ctx\|plan_path\|PlanContext\|parse_plan" runos/ tests/` returns zero non-test-name matches.
  </verify>
  <acceptance_criteria>
    - `grep -c "plan_path\|plan_ctx\|PlanContext\|parse_plan" runos/analysis/runner.py runos/analysis/report.py runos/cli.py runos/sync/daily.py` returns 0
    - `grep -rn "plan_path\|plan_ctx\|PlanContext\|parse_plan" runos/ tests/` returns 0 (or only matches in test names like `test_no_plan_path_attribute_on_settings`)
    - `uv run pytest tests/ -x` exits 0 (FULL suite)
    - `uv run ruff check runos/ tests/` passes
  </acceptance_criteria>
  <done>
    All live code references to plan.md infrastructure are gone; full test suite still passes.
  </done>
</task>

<task type="auto">
  <name>Task 5: Delete plan.md.example, scrub plan.md from .env.example + README + module docstrings</name>
  <files>plan.md.example, .env.example, README.md, runos/analysis/__init__.py, tests/test_analysis_reports.py, tests/test_analyze_cli.py</files>
  <read_first>
    - plan.md.example (to be deleted)
    - .env.example (lines 16, 65-67 per RESEARCH.md section 4 — TWO mentions to scrub: the file-list comment at line 16 + the trailing comment block at 65-67)
    - README.md (lines 239-241 per PATTERNS.md — plan.md mention to update)
    - runos/analysis/__init__.py (lines 5, 16 per RESEARCH.md section 4 — two docstring mentions)
    - tests/test_analysis_reports.py (line 10 docstring + lines 169, 185 per RESEARCH.md section 4 — additional kwarg sites that PATTERNS.md only flagged the first of)
    - tests/test_analyze_cli.py (line 4 docstring per RESEARCH.md section 4)
    - .planning/phases/08-modular-trackers-heat-adaptation/08-RESEARCH.md section 4 "MISSED by PATTERNS.md" table (the exhaustive list of additional sites)
    - .planning/phases/08-modular-trackers-heat-adaptation/08-PATTERNS.md section "MOD: README.md" + "MOD: .env.example"
  </read_first>
  <action>
    1. Delete plan.md.example at repo root.

    2. .env.example: scrub both mentions per RESEARCH.md section 4:
       - Line ~16 (the file-list comment): remove "plan.md" from the file list (it's likely something like "files you read/edit: plan.md, races.md, and generated reports/")
       - Lines ~65-67 (the trailing comment block): rewrite to drop plan.md and add heat.md, per PATTERNS.md "MOD: .env.example" suggestion:
         ```
         # Tracker files (read by analyses for context):
         #   races.md  -- race-readiness context (PLAN-01)
         #   heat.md   -- heat-adaptation sessions (recovery context)
         # RunOS reads them from RUNOS_CONTENT_DIR (default ~/.runos/). Copy the committed
         # *.md.example templates there and edit them. These files are never committed.
         ```

    3. README.md: update lines 239-241 per PATTERNS.md "MOD: README.md" — drop plan.md, add heat.md:
       ```
       **Tracker files**: copy `races.md.example` and `heat.md.example` into your
       content dir as `races.md` / `heat.md` (default `~/.runos/`) and edit them.
       RunOS reads `races.md` for race-readiness context and `heat.md` for
       heat-adaptation context in the recovery report; both are never committed.
       ```

    4. runos/analysis/__init__.py: scrub line 5 (drop "plan.md" mention) and confirm line 16 was updated in Task 1 (races.py reference). Confirm the heat bullet from Plan B is present.

    5. tests/test_analysis_reports.py: rewrite module docstring (line 10) to drop "plan.md" mention; remove any remaining `plan_path=` kwarg sites (Task 4 should have caught these in source but the test file's `_write_context` helper and the test functions themselves may still pass plan paths). Also remove the now-obsolete `_write_context` plan portion if Plan D didn't fully scrub it.

    6. tests/test_analyze_cli.py: rewrite module docstring (line 4) to drop "plan.md" mention; the seeded_cli fixture's `plan_path.write_text(...)` call (RESEARCH.md says line 63) — remove it entirely (Plan D's heat extension stays).

    7. Final sweep: `grep -rn "plan.md\|plan_path\|PlanContext\|parse_plan" runos/ tests/ README.md .env.example` should return ZERO matches outside of test-name strings. (CLAUDE.md is explicitly out of scope per CONTEXT.md — stale phrasing acceptable; do not touch it.)
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -v</automated>
    Full test suite passes (this is the canonical proof of clean retirement).
    Also: `test ! -e plan.md.example && echo "DELETED OK"`
    Also: `grep -rn "plan\.md\|plan_path\|PlanContext\|parse_plan" runos/ tests/ README.md .env.example` returns no matches.
  </verify>
  <acceptance_criteria>
    - `plan.md.example` does not exist at repo root
    - `grep -c "plan\.md\|plan_path\|PlanContext\|parse_plan" .env.example` returns 0
    - `grep -c "plan\.md\|plan_path" README.md` returns 0
    - `grep -c "plan\.md\|plan_path\|PlanContext\|parse_plan" runos/analysis/__init__.py` returns 0
    - `grep -c "heat.md\|heat" .env.example` returns ≥ 1 (the new heat.md mention)
    - `grep -c "heat.md.example\|heat.md" README.md` returns ≥ 1
    - `uv run pytest tests/ -x` exits 0 (FULL suite passes — the ultimate retirement test)
    - `uv run ruff check runos/ tests/` passes
    - `grep -rn "plan\.md\|plan_path\|PlanContext\|parse_plan" runos/ tests/ README.md .env.example` returns ZERO non-test-name matches
  </acceptance_criteria>
  <done>
    plan.md is fully retired; tracker landscape doc surfaces (README + .env.example) reflect just races + heat; test suite green; CLAUDE.md left untouched per CONTEXT.md.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x -v` — full suite passes (canonical check)
- `uv run ruff check runos/ tests/` — clean
- `grep -rn "plan\.md\|plan_path\|PlanContext\|parse_plan\|runos.analysis.context" runos/ tests/ README.md .env.example` — ZERO matches (excluding test-name strings if any)
- `test ! -e plan.md.example -a ! -e runos/analysis/context.py -a ! -e tests/test_context.py` — all three deletions confirmed
- Smoke: `uv run runos analyze race-readiness` against a seeded throwaway DB renders without plan context, without errors
</verification>

<success_criteria>
- plan.md.example deleted
- runos/analysis/context.py deleted (Plan A's shim is gone)
- tests/test_context.py deleted
- parse_plan, PlanContext, _PLAN_FIELD_RE, plan_path, plan_ctx all gone from live source
- All 8 RESEARCH.md section 7 import sites migrated to runos.analysis.races
- README + .env.example reflect tracker landscape: races + heat (no plan)
- Race-readiness report renders cleanly without plan-context section
- Full test suite still passes
- CLAUDE.md untouched (per CONTEXT.md — out of scope)
</success_criteria>

<output>
Create `.planning/phases/08-modular-trackers-heat-adaptation/08-05-SUMMARY.md` when done. Summary must report:
- Confirmation of the 3 file deletions (plan.md.example, runos/analysis/context.py, tests/test_context.py)
- Result of `grep -rn "plan\.md\|plan_path\|PlanContext\|parse_plan\|runos.analysis.context" runos/ tests/ README.md .env.example` (must be empty)
- Final test count vs. start of Phase 8 (delta)
- Final ruff status (clean)
- Any retirement surprises encountered (callers that surfaced during deletion that weren't in RESEARCH.md section 4)
</output>
