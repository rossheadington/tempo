---
phase: 08-modular-trackers-heat-adaptation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tempo/analysis/races.py
  - tempo/analysis/context.py
  - tempo/analysis/__init__.py
  - tests/test_races.py
  - tests/test_context.py
  - races.md.example
autonomous: true
requirements:
  - TRACK-01
  - TRACK-02

must_haves:
  truths:
    - "A line `result: 1:31:48` in races.md surfaces as Race.result == '1:31:48' (verbatim)"
    - "A line `result: DNF` surfaces as Race.result == 'DNF'"
    - "RacesContext.completed(today) returns past-dated races, most-recent-first, excludes undated and today"
    - "RacesContext.upcoming(today) behaviour is unchanged"
    - "races.md.example shows a `result:` example and a `## Past races` section header"
    - "After this plan, `from tempo.analysis.races import parse_races, Race, RacesContext` works; `from tempo.analysis.context import parse_races, Race, RacesContext` also still works (transition shim)"
  artifacts:
    - path: "tempo/analysis/races.py"
      provides: "races.md parser, Race dataclass with result field, RacesContext.upcoming + completed"
      contains: "def parse_races, class Race, class RacesContext, def parse_distance, def parse_goal_time, def _parse_kv, def _parse_race_line"
    - path: "tests/test_races.py"
      provides: "races parser tests (existing) + new result + completed tests"
      contains: "test_parse_races_result_field_verbatim, test_parse_races_result_freeform_strings, test_completed_sorts_most_recent_first, test_completed_excludes_future_and_today, test_completed_excludes_undated_races"
    - path: "races.md.example"
      provides: "user-facing template, now with result: example + ## Past races header"
      contains: "result:, ## Past races"
  key_links:
    - from: "tempo/analysis/context.py"
      to: "tempo/analysis/races.py"
      via: "re-export shim (`from tempo.analysis.races import *`) so other modules continue to work until Plan D rewires them"
      pattern: "from tempo.analysis.races import"
    - from: "Race dataclass"
      to: "_parse_race_line"
      via: "result field added to dataclass and extracted from recognised-keys"
      pattern: "result: str | None"
---

<objective>
Take the deferred `tempo/analysis/context.py` → `tempo/analysis/races.py` rename (Q7), add the optional verbatim `result:` field to the `Race` dataclass, and add `RacesContext.completed(today)` mirror of `upcoming`. Update `races.md.example` with a `result:` example and a `## Past races` section header.

To keep this plan self-contained (Wave 1, no dependency on Plan D's runner/report/recovery wiring), leave `tempo/analysis/context.py` in place as a thin **re-export shim** that imports from the new `tempo/analysis/races.py`. Plan E (cleanup) will delete the shim once all import sites have been switched in Plan D. This preserves the atomic-rename property RESEARCH.md recommended without forcing Plan A to also rewire 8 import sites that other plans already touch.

Purpose: Establish the new `races.py` module + the `result` and `completed()` capabilities so downstream plans (race-link in Plan C, render wiring in Plan D) can import the stable interface from `tempo.analysis.races`.

Output:
- New module `tempo/analysis/races.py` (copied from context.py with edits)
- `tempo/analysis/context.py` becomes a `from tempo.analysis.races import *` shim with `__all__` defined (still exports `parse_plan` and `PlanContext` for now — Plan E removes those)
- New `tests/test_races.py` (split + extended from test_context.py, races portion only)
- `tests/test_context.py` stays as a smaller file asserting the shim continues to re-export the names (it gets fully deleted in Plan E)
- `races.md.example` updated
- `tempo/analysis/__init__.py` module-index updated to mention `races`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-modular-trackers-heat-adaptation/08-CONTEXT.md
@.planning/phases/08-modular-trackers-heat-adaptation/08-RESEARCH.md
@.planning/phases/08-modular-trackers-heat-adaptation/08-PATTERNS.md
@tempo/analysis/context.py
@tempo/analysis/__init__.py
@races.md.example
@tests/test_context.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tempo/analysis/races.py with result field + completed() helper</name>
  <files>tempo/analysis/races.py</files>
  <read_first>
    - tempo/analysis/context.py (lines 1-228, the entire current parser — copy this as the seed for races.py)
    - .planning/phases/08-modular-trackers-heat-adaptation/08-PATTERNS.md section "MOD: tempo/analysis/context.py (surgical edits)" lines 246-306 (exact ADD blocks for result and completed)
    - .planning/phases/08-modular-trackers-heat-adaptation/08-RESEARCH.md section 7 (rename rationale: 8 import sites, plural races.py distinct from existing singular race.py)
    - .planning/phases/08-modular-trackers-heat-adaptation/08-CONTEXT.md "races.md changes" decisions section (lines 39-46)
  </read_first>
  <behavior>
    - parse_races on a missing file returns RacesContext(present=False, races=[], source_path=str(path))
    - parse_races on a file with `- Berlin Marathon - date: 2026-09-27 | distance: marathon | goal: 3:15:00 | priority: A` returns one Race with name=Berlin Marathon, race_date=date(2026,9,27), distance_label='marathon', distance_m=42195.0 (or close), goal='3:15:00', goal_time_s=11700.0, priority='A', result=None
    - parse_races on a file containing a bullet with `result: 1:31:48` populates Race.result == '1:31:48' (verbatim, no parsing into seconds)
    - parse_races on `result: DNF` and `result: 39:42 (course PB)` both store the string verbatim
    - RacesContext(present=True, races=[r1_today, r2_yesterday, r3_tomorrow, r4_undated]).completed(today) returns [r2_yesterday] (excludes today's race, excludes future, excludes undated)
    - RacesContext.completed sorts most-recent-first when multiple past races present
    - RacesContext.upcoming(today) behaviour unchanged (still soonest-first; undated last)
    - The recognised-keys set in _parse_race_line includes 'result'
    - Module exports parse_races, parse_distance, parse_goal_time, Race, RacesContext, _parse_kv, _parse_race_line (the last two as module-private but importable for the heat module if it chooses to share — Plan B decides whether to import or copy)
  </behavior>
  <action>
    Copy tempo/analysis/context.py to tempo/analysis/races.py. In the new file:
    1. Rewrite the module docstring to be races-only (drop the plan.md paragraph and the trailing format example block — keep just the races.md format example).
    2. Delete PlanContext dataclass, _PLAN_FIELD_RE compiled regex, and parse_plan function (the shim in context.py keeps them re-exported from context.py for now).
    3. Add `result: str | None = None` as the last field on the Race dataclass.
    4. In _parse_race_line: add `"result"` to the recognised set; after the priority block, extract `result_str = fields.get("result")` and pass `result=result_str` into the Race(...) constructor.
    5. Add a `completed(self, today: date) -> list[Race]` method to RacesContext, mirroring upcoming but with `r.race_date < today` (strict less-than, excludes today), sorted by race_date reverse=True (most-recent-first), and excluding undated races entirely.
    6. Keep parse_distance, parse_goal_time, _parse_kv, _parse_race_line, parse_races functions intact (with the edits above).
    7. Ensure all imports still resolve (`from tempo.analysis.race import STANDARD_DISTANCES_M` stays — singular `race.py` is the predictions module, distinct from new plural `races.py`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_races.py -x -q</automated>
    Also: `uv run python -c "from tempo.analysis.races import parse_races, Race, RacesContext; r = Race(name='x', result='3:17:42'); assert r.result == '3:17:42'; print('OK')"`
  </verify>
  <acceptance_criteria>
    - File tempo/analysis/races.py exists
    - `grep -n "^def parse_races\|^class Race\|^class RacesContext\|def completed\|result: str" tempo/analysis/races.py` finds parse_races, Race, RacesContext, def completed, and result field
    - `grep -c "PlanContext\|parse_plan\|_PLAN_FIELD_RE" tempo/analysis/races.py` returns 0 (the new module has zero plan references)
    - `uv run python -c "from tempo.analysis.races import parse_races, parse_distance, parse_goal_time, Race, RacesContext"` exits 0
    - `uv run ruff check tempo/analysis/races.py` passes
  </acceptance_criteria>
  <done>
    races.py is the canonical races parser; result and completed are wired; ruff clean; importable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert tempo/analysis/context.py into a transition re-export shim</name>
  <files>tempo/analysis/context.py, tempo/analysis/__init__.py</files>
  <read_first>
    - tempo/analysis/context.py (current full file — to be reduced to a shim)
    - tempo/analysis/__init__.py (current module index — add a races bullet)
    - .planning/phases/08-modular-trackers-heat-adaptation/08-RESEARCH.md section 7 "Import-site inventory" (8 import sites listed verbatim — the shim must keep all of them working until Plan D migrates them and Plan E deletes the shim)
    - .planning/phases/08-modular-trackers-heat-adaptation/08-PATTERNS.md section "MOD: tempo/analysis/__init__.py" (mentioned in NEW heat.py section, line 131)
  </read_first>
  <action>
    1. Replace tempo/analysis/context.py with a thin shim:
       - Module docstring: one paragraph noting this module is a transition shim re-exporting from tempo.analysis.races and tempo.analysis.plan_legacy (see below) and will be deleted in Plan E.
       - `from tempo.analysis.races import parse_races, parse_distance, parse_goal_time, Race, RacesContext, _parse_kv, _parse_race_line`
       - Keep the PlanContext dataclass, _PLAN_FIELD_RE, and parse_plan function INLINE in this shim (do NOT move them to a new module — they are being deleted in Plan E and copying them to a new file would just add a deletion site). This means context.py shrinks from 228 lines to roughly the plan portion (PlanContext class, _PLAN_FIELD_RE, parse_plan) plus the re-exports.
       - Add `__all__ = ["parse_races", "parse_distance", "parse_goal_time", "Race", "RacesContext", "PlanContext", "parse_plan"]`
    2. Update tempo/analysis/__init__.py docstring (lines 11-19 module index): add a bullet `* :mod:`tempo.analysis.races` -- parse `races.md`.` after the existing context bullet (or replace the context bullet — context.py is now transitional). Leave the rest of the index unchanged; Plan B will add the heat bullet.
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q -k "context or races"</automated>
    Also smoke-test legacy imports continue to work: `uv run python -c "from tempo.analysis.context import parse_races, Race, RacesContext, parse_plan, PlanContext; print('OK')"`
  </verify>
  <acceptance_criteria>
    - tempo/analysis/context.py exists and is ≤ 60 lines (shim + plan portion only)
    - `grep -c "from tempo.analysis.races import" tempo/analysis/context.py` returns 1 (the re-export line)
    - `uv run python -c "from tempo.analysis.context import parse_races, Race, RacesContext, parse_plan, PlanContext; print('OK')"` exits 0
    - `uv run python -c "from tempo.analysis import context as ctx; assert callable(ctx.parse_races); assert callable(ctx.parse_plan); print('OK')"` exits 0 (covers the `from tempo.analysis import context as ctx` import sites)
    - All existing tests in tests/test_context.py still pass (they import from tempo.analysis.context, which the shim re-exports)
    - `uv run ruff check tempo/analysis/context.py tempo/analysis/__init__.py` passes
  </acceptance_criteria>
  <done>
    context.py is a thin transition shim; all 8 existing import sites continue to resolve unchanged; plan.md parser still works (will be removed in Plan E).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/test_races.py with new result + completed tests, leave tests/test_context.py minimal</name>
  <files>tests/test_races.py, tests/test_context.py</files>
  <read_first>
    - tests/test_context.py (current file — lines 1-95 are races tests; 96-134 are plan tests; lines 85-95 is the upcoming sort model)
    - .planning/phases/08-modular-trackers-heat-adaptation/08-RESEARCH.md section 6 "Test Coverage Outline" subsections "races.md result: field" (Res1/Res2) and "RacesContext.completed(today)" (C1/C2/C3)
    - .planning/phases/08-modular-trackers-heat-adaptation/08-PATTERNS.md section "MOD: tests/test_context.py (split + extend)" lines 615-668
  </read_first>
  <behavior>
    - test_parse_races_missing_file: parse_races(nonexistent path) returns RacesContext(present=False, races=[])
    - test_parse_races_basic: well-formed bullet parses into a Race with all expected fields populated
    - test_parse_races_ignores_prose_and_headings: doc bullets (no recognised key) are not treated as races; lines starting with # are skipped
    - test_parse_races_lenient_partial_fields: missing optional fields are None on the dataclass; no crash
    - test_parse_races_bad_date_keeps_race: a bullet with `date: tomorrow` keeps the race with race_date=None (existing behaviour — call out for contrast with heat which drops the entry)
    - test_upcoming_sorts_and_filters: existing behaviour preserved
    - NEW test_parse_races_result_field_verbatim: `result: 1:31:48` lands on Race.result == "1:31:48" with no parsing into seconds
    - NEW test_parse_races_result_freeform_strings: `result: DNF` → "DNF"; `result: 39:42 (course PB)` → "39:42 (course PB)"; both verbatim with no normalisation
    - NEW test_completed_sorts_most_recent_first: three past-dated races returned in reverse-chronological order
    - NEW test_completed_excludes_future_and_today: a race dated today is NOT in completed (strict <); a future race is excluded
    - NEW test_completed_excludes_undated_races: a Race with race_date=None does not appear in completed
  </behavior>
  <action>
    1. Create tests/test_races.py:
       - Module docstring: "Parsing of races.md, including the missing-file path, the result field, and the upcoming/completed helpers."
       - Imports: `from tempo.analysis.races import parse_distance, parse_goal_time, parse_races` (import from races, not context — proves the new module is the canonical home).
       - Copy the races portion of tests/test_context.py verbatim (the missing-file, basic, prose, partial-fields, bad-date, distance/goal helper tests, and test_upcoming_sorts_and_filters).
       - Add the 5 new tests above (Res1, Res2, C1, C2, C3) per the RESEARCH section-6 spec.
    2. Reduce tests/test_context.py to:
       - Module docstring noting it tests the transition shim
       - 2-3 small tests asserting the shim still re-exports the names: `test_context_shim_reexports_races`, `test_context_shim_still_exports_parse_plan_for_now`. Plan E deletes this entire file.
       - Drop ALL parser-behaviour tests (those now live in test_races.py).
       - Keep the plan-portion tests (test_parse_plan_missing_file, test_parse_plan_captures_headings_and_fields, test_parse_plan_first_field_wins) — Plan E deletes them along with the parser.
  </action>
  <verify>
    <automated>uv run pytest tests/test_races.py tests/test_context.py -x -v</automated>
    All 5 new tests must appear and pass; existing races tests must still pass; existing plan tests must still pass (until Plan E).
  </verify>
  <acceptance_criteria>
    - File tests/test_races.py exists
    - `grep -c "^def test_parse_races_result_field_verbatim\|^def test_parse_races_result_freeform_strings\|^def test_completed_sorts_most_recent_first\|^def test_completed_excludes_future_and_today\|^def test_completed_excludes_undated_races" tests/test_races.py` returns 5
    - `uv run pytest tests/test_races.py -v --collect-only 2>&1 | grep -c "test_"` is ≥ 10 (the 5 new + at least 5 carried-over races tests)
    - `uv run pytest tests/test_races.py tests/test_context.py -x` exits 0
    - `uv run ruff check tests/test_races.py tests/test_context.py` passes
  </acceptance_criteria>
  <done>
    test_races.py covers parser + result + completed; test_context.py reduced to shim-asserting tests + the plan-portion (both deleted in Plan E).
  </done>
</task>

<task type="auto">
  <name>Task 4: Update races.md.example with result: example and ## Past races section header</name>
  <files>races.md.example</files>
  <read_first>
    - races.md.example (current file, lines 1-27)
    - .planning/phases/08-modular-trackers-heat-adaptation/08-CONTEXT.md "races.md.example updates" section (lines 92-95)
    - .planning/phases/08-modular-trackers-heat-adaptation/08-PATTERNS.md section "MOD: races.md.example (template additions)" lines 595-612
  </read_first>
  <action>
    1. Add a recognised-keys bullet for `result` between the existing `goal` and `priority` lines (or after `priority` — order is documentation only, parser doesn't care):
       `- \`result\` — free-form finish-time string for a past race, e.g. \`3:17:42\`, \`1:32:11\`, \`DNF\`, \`39:42 (course PB)\`. Stored verbatim; no comparison logic.`
    2. After the existing examples (or at the bottom of the file), add a `## Past races` section header (parser ignores `#` lines, so this is human-readable only) with one past-race example bullet:
       `- Local Half - date: 2026-04-12 | distance: half | goal: 1:32:00 | priority: B | result: 1:31:48`
    3. Keep the existing upcoming examples intact under the existing section header.
  </action>
  <verify>
    <automated>grep -c "result:" races.md.example</automated>
    Expect ≥ 2 (one in the recognised-keys list, one in the past-race example).
    Also: `grep -c "^## Past races" races.md.example` returns 1.
    Also: `uv run python -c "from pathlib import Path; from tempo.analysis.races import parse_races; ctx = parse_races(Path('races.md.example')); past = [r for r in ctx.races if r.result]; assert any(r.result == '1:31:48' for r in past), [r.result for r in past]; print('OK')"` exits 0 (the example itself parses cleanly).
  </verify>
  <acceptance_criteria>
    - races.md.example contains a `result:` recognised-keys bullet
    - races.md.example contains a `## Past races` section header
    - races.md.example contains at least one past-race example bullet with a `result:` field
    - Parsing races.md.example produces at least one Race with result != None
  </acceptance_criteria>
  <done>
    The committed template shows users the new optional result field and the past-races convention.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_races.py tests/test_context.py -x -v` — all pass
- `uv run pytest tests/ -x` — full suite still passes (the shim keeps all existing import sites working)
- `uv run ruff check tempo/analysis/races.py tempo/analysis/context.py tempo/analysis/__init__.py tests/test_races.py tests/test_context.py` — clean
- Smoke: `uv run python -c "from tempo.analysis.races import parse_races, Race; from tempo.analysis.context import parse_plan; from tempo.analysis import context as ctx; print('shim ok')"`
</verification>

<success_criteria>
- New canonical module tempo/analysis/races.py exists with result field on Race and completed() on RacesContext
- tempo/analysis/context.py is a thin shim that keeps all 8 import sites listed in 08-RESEARCH.md section 7 working
- tests/test_races.py covers the 5 new behaviours (Res1, Res2, C1, C2, C3) plus the carried-over races tests
- races.md.example shows the new result: convention and the ## Past races header
- Full test suite still passes; ruff clean
- No regression in upcoming() behaviour
</success_criteria>

<output>
Create `.planning/phases/08-modular-trackers-heat-adaptation/08-01-SUMMARY.md` when done. Summary must report:
- Whether the rename was clean (any test failures during the cut-over, any shim caveats discovered)
- Confirmation that tests/test_races.py has the 5 new tests passing
- File counts: tempo/analysis/races.py line count, tempo/analysis/context.py line count after shim conversion
</output>
