---
phase: 12-lifecycle-hardening-privacy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - launchd/com.runos.telegram-bot.plist
  - runos/scheduler.py
  - runos/cli.py
  - runos/config.py
  - runos/bot/handlers.py
  - runos/bot/app.py
  - tests/test_telegram_scheduler.py
  - tests/test_voice_retention.py
  - tests/test_bot_cli.py
  - .gitignore
autonomous: true
requirements: [VOICE-11, VOICE-14, VOICE-15]
tags: [telegram, launchd, privacy, lifecycle]

must_haves:
  truths:
    - "A committed `launchd/com.runos.telegram-bot.plist` template exists, is valid XML, parses via plistlib, and contains Label=com.runos.telegram-bot + KeepAlive=true + ThrottleInterval=10 + RunAtLoad=true."
    - "Running `runos bot install-scheduler` writes a resolved (absolute-path) plist to the user's chosen location and prints the launchctl bootstrap/bootout commands -- RunOS NEVER runs launchctl itself."
    - "After a successful voice transcription + agent reply, the cached .ogg file is deleted by default (voice_retention_days=0); when voice_retention_days>0 the file is kept and a startup sweep removes files older than the window."
    - "Running `runos bot purge-voice --yes` removes every file under <content_dir>/voice/."
    - "At startup (post_init), the bot logs a line of the form `agent cwd = <abs-path>` so the user can confirm the working dir launchd set."
  artifacts:
    - path: "launchd/com.runos.telegram-bot.plist"
      provides: "Committed plist template (placeholders, secret-free)"
      contains: "com.runos.telegram-bot"
    - path: "runos/scheduler.py"
      provides: "render_bot_plist + install_bot_plist helpers (new functions)"
      exports: ["render_bot_plist", "install_bot_plist", "BOT_LABEL"]
    - path: "runos/config.py"
      provides: "voice_retention_days Settings field"
      contains: "voice_retention_days"
    - path: "runos/bot/handlers.py"
      provides: "_cleanup_voice_file helper invoked after successful agent turn"
      contains: "_cleanup_voice_file"
    - path: "runos/bot/app.py"
      provides: "Startup voice-cache sweep + `agent cwd = ...` log line in _post_init"
      contains: "agent cwd ="
    - path: "runos/cli.py"
      provides: "runos bot install-scheduler + runos bot purge-voice subcommands"
      contains: "install-scheduler"
  key_links:
    - from: "runos/cli.py (`bot install-scheduler`)"
      to: "runos/scheduler.py (`install_bot_plist`)"
      via: "direct function call rendering + writing the plist; never invokes launchctl"
      pattern: "install_bot_plist"
    - from: "runos/bot/handlers.py (voice_handler tail)"
      to: "runos/bot/handlers.py (`_cleanup_voice_file`)"
      via: "call after `_run_agent_turn` completes successfully"
      pattern: "_cleanup_voice_file"
    - from: "runos/bot/app.py (_post_init)"
      to: "runos/bot/handlers.py (voice cache sweep helper)"
      via: "startup pass over <content_dir>/voice/ using settings.voice_retention_days"
      pattern: "sweep_voice_cache|voice cache sweep"
---

<objective>
Ship the lifecycle + privacy half of Phase 12: a committed launchd LaunchAgent template for the Telegram bot, a `runos bot install-scheduler` CLI that renders it with absolute paths, a configurable voice-file retention policy (delete-on-success default + startup sweep for keep-N-days), a `runos bot purge-voice` helper, and a startup log line confirming the agent's working directory.

Purpose: VOICE-11 (the bot survives crashes / sleep / wake / reboot via `KeepAlive=true`), VOICE-14 (voice files don't accumulate -- audio stays on the laptop only as long as the user wants), and VOICE-15 (the agent's cwd is the RunOS project root, visible and verifiable). This is the always-on plumbing that turns the Phase 11 bot from a "runs while a terminal is open" tool into a real background service.

Output: a valid plist template + installer + the retention machinery + tests proving each piece behaves correctly. Wave 1 -- no dependency on the error-handler work in plan 12-02 (the only shared file is `runos/bot/app.py`, so 12-02 runs in Wave 2 after this).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-lifecycle-hardening-privacy/12-CONTEXT.md
@CLAUDE.md
@launchd/com.runos.daily.plist
@runos/scheduler.py
@runos/cli.py
@runos/config.py
@runos/bot/app.py
@runos/bot/handlers.py
@tests/test_scheduler.py

<interfaces>
<!-- Key types and contracts the executor will use. Extracted from the codebase
     so no scavenger-hunt is needed. -->

From `runos/scheduler.py` (existing — DO NOT BREAK; add new helpers ALONGSIDE):
```python
LABEL = "com.runos.daily"  # existing daily-job label; ADD a sibling BOT_LABEL = "com.runos.telegram-bot"

@dataclass(frozen=True, slots=True)
class SchedulerPaths:
    program: str
    args: list[str]
    working_dir: str
    stdout_log: str
    stderr_log: str
    data_dir: str

def _find_program(project_dir: Path) -> tuple[str, list[str]]: ...
def resolve_paths(*, project_dir: Path, data_dir: Path) -> SchedulerPaths: ...
def render_plist(paths: SchedulerPaths, *, hour: int = 5, minute: int = 30, label: str = LABEL) -> str: ...
def install_plist(*, project_dir: Path, data_dir: Path, hour: int = 5, minute: int = 30,
                  to_launch_agents: bool = False, out_dir: Path | None = None,
                  label: str = LABEL) -> InstallResult: ...
def launch_agents_dir() -> Path: ...
def plist_filename(label: str = LABEL) -> str: ...

@dataclass(frozen=True, slots=True)
class InstallResult:
    plist_path: Path
    installed_to_launch_agents: bool
    load_command: str
    unload_command: str
```

From `runos/config.py` (existing Settings, partial):
```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="RUNOS_", env_file=".env", ...)
    # ...
    @property
    def content_root(self) -> Path: ...
    @property
    def voice_cache_dir(self) -> Path:  # = self.content_root / "voice"
        ...
```

From `runos/bot/handlers.py` (existing voice_handler tail, simplified):
```python
async def voice_handler(update, context) -> None:
    # ... download to target_path ...
    transcript = await asyncio.to_thread(transcribe_file, target_path)
    # ... empty-transcript short circuit ...
    chat_id = update.effective_chat.id
    await _run_agent_turn(update, context, transcript, chat_id)
    # << NEW: _cleanup_voice_file(target_path, settings) here
```

From `runos/bot/app.py` (existing `_post_init`):
```python
async def _post_init(application: Application) -> None:
    await application.bot.delete_webhook(drop_pending_updates=False)
    conn = await asyncio.to_thread(init_db, settings.db_path)
    conn.close()
    await asyncio.to_thread(warm_model, settings)
    logger.info("Whisper model loaded and ready")
    # << NEW: sweep voice cache + log `agent cwd = ...`
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: launchd plist template + install-scheduler + cwd log + .gitignore (VOICE-11, VOICE-15)</name>
  <files>launchd/com.runos.telegram-bot.plist, runos/scheduler.py, runos/cli.py, runos/bot/app.py, .gitignore, tests/test_telegram_scheduler.py</files>
  <read_first>
    - `launchd/com.runos.daily.plist` -- the existing template; mirror its shape (DTD line, XML structure, `Label`/`ProgramArguments`/`WorkingDirectory`/`EnvironmentVariables`/`StandardOutPath`/`StandardErrorPath` layout, `RunAtLoad` style).
    - `runos/scheduler.py` -- existing `LABEL`, `SchedulerPaths`, `_find_program`, `resolve_paths`, `render_plist`, `install_plist`, `InstallResult`. The new helpers MUST live alongside (not replace) these. Reuse `_find_program` / `launch_agents_dir` / `plist_filename` / `write_plist` directly.
    - `runos/cli.py` -- the existing `install_scheduler_cmd` (around the `@app.command("install-scheduler")` decorator) for the typer prompts/output style and how it calls `scheduler.install_plist`. The new `bot install-scheduler` mirrors this on `bot_app` (the existing `bot_app = typer.Typer(...)` block).
    - `runos/bot/app.py` -- the `_post_init` async function inside `build_application`; the new cwd log line goes here.
    - `tests/test_scheduler.py` -- existing pattern for `plistlib.loads(...)` + `plutil -lint` validation; mirror the structure.
  </read_first>
  <behavior>
    Plist template (committed):
      - File `launchd/com.runos.telegram-bot.plist` exists, is valid XML with the Apple DOCTYPE, and `plistlib.loads(...)` over it (with `__PROJECT_DIR__` / `__UV_BIN__` / `__TZ__` / `__DATA_DIR__` placeholders left as strings) returns a dict with: `Label == "com.runos.telegram-bot"`, `ProgramArguments == ["__UV_BIN__", "run", "runos", "bot", "run"]`, `WorkingDirectory == "__PROJECT_DIR__"`, `EnvironmentVariables["PATH"]` includes `/opt/homebrew/bin`, `EnvironmentVariables["OMP_NUM_THREADS"] == "4"`, `EnvironmentVariables["TZ"] == "__TZ__"`, `RunAtLoad is True`, `KeepAlive is True`, `ThrottleInterval == 10`, `StandardOutPath` ends with `logs/telegram-bot.stdout.log`, `StandardErrorPath` ends with `logs/telegram-bot.stderr.log`. If `plutil` is on PATH, running `plutil -lint <committed-template>` exits 0.

    Scheduler module additions (`runos/scheduler.py`):
      - `BOT_LABEL = "com.runos.telegram-bot"` constant.
      - `render_bot_plist(*, project_dir: Path, uv_bin: str, data_dir: Path, tz: str = "Europe/London") -> str` -- returns the resolved (no placeholders) plist text. Reuses `xml.sax.saxutils.escape` like the existing `render_plist`. Logs path is `<project_dir>/logs/telegram-bot.{stdout,stderr}.log`. KeepAlive=true, ThrottleInterval=10, RunAtLoad=true.
      - `install_bot_plist(*, project_dir: Path, data_dir: Path, tz: str = "Europe/London", to_launch_agents: bool = False, out_dir: Path | None = None) -> InstallResult` -- resolves `uv_bin = shutil.which("uv")` (raises a clear `RuntimeError("`uv` not found on PATH ...")` if None), renders via `render_bot_plist`, writes via `write_plist`. Returns `InstallResult` with `load_command = f"launchctl bootstrap gui/{os.getuid()} {final_path}"` and `unload_command = f"launchctl bootout gui/{os.getuid()} {final_path}"`. Uses `BOT_LABEL` for the filename.

    CLI additions (`runos/cli.py`):
      - New typer command on the existing `bot_app`: `runos bot install-scheduler [--tz TZ] [--to-launch-agents]`.
        - Default `tz = "Europe/London"`; default `to_launch_agents = False` (writes template under `<data_dir>/launchd/`).
        - Calls `scheduler.install_bot_plist(...)`; prints the resolved plist path + launchctl bootstrap/bootout commands; explicitly prints a line that says RunOS did NOT run launchctl.
        - If `shutil.which("plutil")` is available, runs `subprocess.run(["plutil", "-lint", str(result.plist_path)], check=False, capture_output=True, text=True)` and prints `plutil -lint: PASS` or `FAIL: <stderr>`. Exit 1 on FAIL.
      - DO NOT touch the existing `install_scheduler_cmd` (daily job). Both commands coexist.

    bot/app.py cwd log (`_post_init`):
      - Add (immediately after `logger.info("Whisper model loaded and ready")`): `logger.info("agent cwd = %s", str(Path.cwd().resolve()))`. Use `from pathlib import Path` (already imported elsewhere; add at top if missing).

    .gitignore:
      - Add a line `logs/` (under a comment `# ---- launchd telegram-bot logs (project-local) ----`) so the plist's `<project>/logs/` directory never gets committed. Place it under the existing "Personal health data" block or as its own section -- pick a sensible spot.

    Tests (`tests/test_telegram_scheduler.py` -- NEW file):
      - `test_committed_template_parses`: read `launchd/com.runos.telegram-bot.plist` from the repo root, `plistlib.loads`, assert Label / KeepAlive / ThrottleInterval / RunAtLoad / ProgramArguments / EnvironmentVariables keys per the behavior above.
      - `test_committed_template_plutil_lint`: skip if `shutil.which("plutil") is None`; else run `subprocess.run(["plutil", "-lint", <committed-template>], ...)`, assert returncode == 0.
      - `test_render_bot_plist_resolves_placeholders`: call `scheduler.render_bot_plist(project_dir=tmp_path/"proj", uv_bin="/opt/homebrew/bin/uv", data_dir=tmp_path/"data", tz="UTC")`; `plistlib.loads(text.encode())`; assert `Label=="com.runos.telegram-bot"`, `ProgramArguments[0]=="/opt/homebrew/bin/uv"`, `EnvironmentVariables["TZ"]=="UTC"`, `KeepAlive is True`, `ThrottleInterval==10`, `RunAtLoad is True`.
      - `test_install_bot_plist_writes_resolved_template`: monkeypatch `shutil.which` so `which("uv")` returns `/usr/bin/uv`; call `scheduler.install_bot_plist(project_dir=tmp_path/"proj", data_dir=tmp_path/"data", tz="UTC", out_dir=tmp_path/"out")`; assert the resolved plist exists, parses, and its `WorkingDirectory == str(tmp_path/"proj")`.
      - `test_install_bot_plist_launchctl_commands_use_bootstrap_bootout`: assert `result.load_command.startswith("launchctl bootstrap gui/")` and `result.unload_command.startswith("launchctl bootout gui/")`.
      - `test_install_bot_plist_raises_when_uv_missing`: monkeypatch `shutil.which("uv")` to None; assert `RuntimeError` mentioning `uv`.
      - `test_cli_bot_install_scheduler` (CliRunner): invoke `runos bot install-scheduler` in an isolated `RUNOS_DATA_DIR`, assert exit 0, assert stdout mentions `launchctl bootstrap`.
  </behavior>
  <action>
    Use the Write tool to create `launchd/com.runos.telegram-bot.plist` from scratch -- model it on `launchd/com.runos.daily.plist` but: `Label=com.runos.telegram-bot`; `ProgramArguments=[__UV_BIN__, run, runos, bot, run]`; no `StartCalendarInterval` (the bot is long-running); `WorkingDirectory=__PROJECT_DIR__`; `EnvironmentVariables` with `PATH` (Homebrew + system), `OMP_NUM_THREADS=4`, `TZ=__TZ__`; `StandardOutPath=__PROJECT_DIR__/logs/telegram-bot.stdout.log` and `StandardErrorPath=__PROJECT_DIR__/logs/telegram-bot.stderr.log`; `RunAtLoad=true`; `KeepAlive=true`; `ThrottleInterval=10`. Keep the helpful comment block at the top explaining the placeholders + bootstrap/bootout commands. The template MUST parse via `plistlib.loads` even with the `__...__` placeholders left as raw strings (they're valid `<string>` values).

    Edit `runos/scheduler.py` to add the new symbols at the bottom of the file (after the existing `install_plist`): `BOT_LABEL`, `render_bot_plist`, `install_bot_plist`. Use `shutil.which("uv")` inside `install_bot_plist` and raise `RuntimeError("`uv` not found on PATH -- install it (https://docs.astral.sh/uv/) before generating the bot launchd plist.")` if missing. Reuse the existing `write_plist`, `launch_agents_dir`, and `plist_filename(BOT_LABEL)`. For the launchctl commands, use `os.getuid()` (add `import os` if missing) and `bootstrap`/`bootout` -- NOT `load`/`unload` -- because that is the modern launchd command set the CONTEXT specifies.

    Edit `runos/cli.py` to add a new `@bot_app.command("install-scheduler")` typer command. Mirror the prose / colour conventions of the existing `install_scheduler_cmd`. Run `subprocess.run(["plutil", "-lint", str(result.plist_path)], check=False, capture_output=True, text=True)` when `plutil` is available; print PASS or FAIL; exit 1 on FAIL.

    Edit `runos/bot/app.py` `_post_init` to add one new INFO log line `logger.info("agent cwd = %s", str(Path.cwd().resolve()))` immediately after the existing `logger.info("Whisper model loaded and ready")`. Verify `from pathlib import Path` is at the top of the file; add it if not (it's used in `runos.bot.handlers` already; `app.py` does not currently import it).

    Edit `.gitignore` to add `logs/` under a new short comment header so the per-project launchd log directory stays out of the repo.

    Use Write to create `tests/test_telegram_scheduler.py` containing the seven tests in `<behavior>`. Use `typer.testing.CliRunner` for the CLI test (matches the project's existing test style; see `tests/test_scheduler.py` and other CLI tests).

    Do NOT use `cat <<EOF` for any of this -- everything via the Write or Edit tool.
  </action>
  <verify>
    <automated>uv run pytest tests/test_telegram_scheduler.py tests/test_scheduler.py -x -q && uv run ruff check launchd/com.runos.telegram-bot.plist runos/scheduler.py runos/cli.py runos/bot/app.py tests/test_telegram_scheduler.py 2>&1 | grep -v 'plist' ; uv run ruff check runos tests -q</automated>
  </verify>
  <done>
    `launchd/com.runos.telegram-bot.plist` exists and is a valid plist (plistlib + plutil-lint where available). `runos bot install-scheduler` resolves placeholders, writes the plist, prints `launchctl bootstrap gui/...` commands, and prints PASS for plutil-lint. The bot logs `agent cwd = <abs-path>` once at startup. `logs/` is gitignored. New tests added; full suite (existing 468 + new) is green; ruff clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: voice_retention_days setting + cleanup helpers + purge-voice CLI (VOICE-14)</name>
  <files>runos/config.py, runos/bot/handlers.py, runos/bot/app.py, runos/cli.py, tests/test_voice_retention.py, tests/test_bot_cli.py</files>
  <read_first>
    - `runos/config.py` -- the existing `Settings` class and `validation_alias` pattern used by Telegram + Whisper fields (`telegram_bot_token`, `whisper_model_name`, etc.). The new `voice_retention_days` should follow the same `validation_alias` style if a bare env var name is preferred, or use the default `RUNOS_` prefix. CONTEXT specifies `validation_alias="VOICE_RETENTION_DAYS"`.
    - `runos/bot/handlers.py` -- the full `voice_handler`. The cleanup call goes after `await _run_agent_turn(update, context, transcript, chat_id)`. Note: `_run_agent_turn` can early-return on `AgentInvocationError` (it sends the missing-CLI reply and returns). The cleanup should still run in that case -- the transcription succeeded; only the agent failed. Implement by wrapping the agent call in a try/finally OR by calling cleanup unconditionally after.
    - `runos/bot/app.py` -- the `_post_init` async function. The voice-cache sweep goes after the cwd log line from Task 1.
    - `runos/config.py` `voice_cache_dir` property = `content_root / "voice"`.
  </read_first>
  <behavior>
    Settings field (`runos/config.py`):
      - New field `voice_retention_days: int = Field(default=0, validation_alias="VOICE_RETENTION_DAYS", description="Days to keep cached voice .ogg files. 0 (default) = delete on successful transcription. Positive = keep N days then sweep at startup.")`. Bare env-var name (no `RUNOS_` prefix), matching the existing Telegram/Whisper fields' convention.
      - Validate: `voice_retention_days >= 0`; raise on negative (use `field_validator` mode="after").

    Cleanup helper + voice_handler integration (`runos/bot/handlers.py`):
      - New private helper `def _cleanup_voice_file(path: Path, retention_days: int) -> None`:
        - If `retention_days == 0`: `path.unlink(missing_ok=True)`; log INFO `"voice cache: deleted %s (retention=0)"`.
        - If `retention_days > 0`: log DEBUG `"voice cache: kept %s (retention=%d days)"`; do nothing.
        - Swallow `OSError` so a cleanup failure does NOT propagate -- log it at WARNING with the path.
      - In `voice_handler`, after `await _run_agent_turn(update, context, transcript, chat_id)` (and the empty-transcript short-circuit's `return` path also, because the file IS still on disk there), call `_cleanup_voice_file(target_path, settings.voice_retention_days)`. Easiest shape: wrap the post-download body in a `try: ... finally: _cleanup_voice_file(target_path, settings.voice_retention_days)`. Confirm `settings` is already in scope (it's read from `context.application.bot_data["settings"]`).

    Startup sweep helper (`runos/bot/handlers.py` -- new module-level function so app.py can import it):
      - `def sweep_voice_cache(voice_cache_dir: Path, retention_days: int, *, now: float | None = None) -> int`:
        - If `retention_days <= 0`: return 0 immediately (no sweep needed -- delete-on-success policy already handles it).
        - If the dir doesn't exist: return 0.
        - For each file in the dir, if `os.path.getmtime(file) < (now or time.time()) - retention_days*86400`, delete it. Count deletions.
        - Returns the deletion count. Logs `voice cache sweep: deleted N files older than %d days`.
        - Swallow per-file OSError with WARNING.

    bot/app.py startup wiring (`_post_init`):
      - After the cwd log line from Task 1, call `from runos.bot.handlers import sweep_voice_cache; await asyncio.to_thread(sweep_voice_cache, settings.voice_cache_dir, settings.voice_retention_days)`.

    purge-voice CLI (`runos/cli.py`):
      - New `@bot_app.command("purge-voice")` typer command: `runos bot purge-voice [--yes]`.
        - Read `settings.voice_cache_dir`. Enumerate files; sum sizes; print a summary line `Voice cache: <N> files, <X.X> MB at <path>`.
        - If `--yes`: delete all files unconditionally, print `Deleted N files.`, exit 0.
        - If not `--yes`: print the summary + `Re-run with --yes to delete.` and exit 0 (no destructive action). Do NOT use `typer.confirm` (interactive prompts make automated testing brittle; the `--yes` flag is the confirmation).
        - If the dir doesn't exist or is empty: print `Voice cache empty at <path>.` and exit 0.

    Tests (`tests/test_voice_retention.py` -- NEW):
      - `test_voice_retention_days_default_is_zero`: instantiate `Settings()` with the env unset; assert `settings.voice_retention_days == 0`.
      - `test_voice_retention_days_reads_env_var`: monkeypatch `VOICE_RETENTION_DAYS=7`; assert `Settings().voice_retention_days == 7`.
      - `test_voice_retention_rejects_negative`: monkeypatch `VOICE_RETENTION_DAYS=-1`; assert `Settings()` raises `ValidationError`.
      - `test_cleanup_voice_file_retention_zero_deletes`: create a tmp .ogg file; call `_cleanup_voice_file(path, 0)`; assert `path.exists() is False`.
      - `test_cleanup_voice_file_retention_positive_keeps`: tmp .ogg; `_cleanup_voice_file(path, 7)`; assert `path.exists() is True`.
      - `test_cleanup_voice_file_swallows_oserror`: pass a non-existent path with `retention_days=0` (the `missing_ok=True` should keep it silent); function returns without raising.
      - `test_sweep_voice_cache_deletes_old_files`: create three tmp files, set their mtimes to (now-10d, now-5d, now-1d); `sweep_voice_cache(dir, retention_days=3, now=time.time())`; assert the two older are gone, the newest survives, returns 2.
      - `test_sweep_voice_cache_zero_retention_is_noop`: same fixture; `retention_days=0`; assert all files survive, returns 0.
      - `test_sweep_voice_cache_missing_dir_is_noop`: pass `Path("/nonexistent/...")`; returns 0; no raise.
      - `test_voice_handler_deletes_file_after_success`: minimal voice-handler integration test using the existing test patterns (look at `tests/test_voice_handler.py` if present, otherwise a focused unit test that exercises `voice_handler` against a fake Telegram Update + fake transcribe + fake `_run_agent_turn`). The .ogg file in `voice_cache_dir` must NOT exist after the handler returns when `voice_retention_days=0`.
      - `test_voice_handler_keeps_file_after_success_with_retention`: same as above with `voice_retention_days=7`; the file MUST still exist.

    Tests (`tests/test_bot_cli.py` -- NEW or extend if exists):
      - `test_bot_purge_voice_dry_run`: `CliRunner` invokes `runos bot purge-voice` (no `--yes`) against a tmp voice dir with 2 files; assert exit 0, stdout mentions both files, files still exist on disk.
      - `test_bot_purge_voice_yes_deletes`: same fixture; invoke with `--yes`; assert exit 0, stdout `Deleted 2 files.`, dir is empty.
      - `test_bot_purge_voice_empty_dir`: empty dir; invoke with or without `--yes`; assert exit 0 and stdout mentions `empty`.
  </behavior>
  <action>
    Edit `runos/config.py` to add the `voice_retention_days` field with `validation_alias="VOICE_RETENTION_DAYS"`. Add a `@field_validator("voice_retention_days", mode="after")` that rejects negatives with `ValueError("VOICE_RETENTION_DAYS must be >= 0")`.

    Edit `runos/bot/handlers.py` to add `_cleanup_voice_file` (private) and `sweep_voice_cache` (public — app.py imports it). Wrap the post-download body of `voice_handler` in a `try/finally` so cleanup runs even when `_run_agent_turn` early-returns on `AgentInvocationError` or when the empty-transcript branch fires (do this by restructuring the function to `try: ... finally: _cleanup_voice_file(target_path, settings.voice_retention_days)` -- preserve every existing log line and reply path verbatim). Per VOICE-14, `_cleanup_voice_file` is per D-VOICE-14 (CONTEXT.md `### Voice-file retention`).

    Edit `runos/bot/app.py` `_post_init` to import `sweep_voice_cache` from `runos.bot.handlers` (at the top of the file alongside the other handler imports) and add `await asyncio.to_thread(sweep_voice_cache, settings.voice_cache_dir, settings.voice_retention_days)` after the cwd log line. The whole startup sequence is: delete_webhook → init_db → warm_model → log `Whisper ... ready` → log `agent cwd = ...` → sweep voice cache.

    Edit `runos/cli.py` to add the `@bot_app.command("purge-voice")` typer command per the behavior section.

    Use Write to create the two new test files. For the voice_handler integration tests, mirror the fake-Update + fake-context pattern from any existing handler test (e.g. `tests/test_voice_handler.py` or similar -- look for existing tests of `voice_handler`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_voice_retention.py tests/test_bot_cli.py tests/test_voice_handler.py tests/ -x -q && uv run ruff check runos tests -q</automated>
  </verify>
  <done>
    `voice_retention_days` Settings field reads `VOICE_RETENTION_DAYS` (default 0, rejects negative). `voice_handler` deletes the .ogg in the default `retention=0` case (verified by integration test), keeps it for `retention>0`. Startup sweep removes files older than the window. `runos bot purge-voice [--yes]` works. All new tests pass; full suite green; ruff clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram client → bot worker | Untrusted update payloads (voice memos with arbitrary metadata, text messages) cross here. Already mitigated by `filters.Chat(owner_chat_id)` from Phase 9 + defense-in-depth re-check in every handler. |
| launchd → runos bot process | Stripped env; absolute paths mandatory. Mitigated by `EnvironmentVariables.PATH` in the plist. |
| voice cache directory → other local users | Mode 0700 already set on `voice_cache_dir` in `voice_handler`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | Cached `.ogg` voice files accumulating on disk indefinitely | mitigate | VOICE-14: default `voice_retention_days=0` deletes file after successful transcription; positive values trigger startup sweep; `runos bot purge-voice` for manual wipe. |
| T-12-02 | Denial of Service | KeepAlive=true could cause tight crash loop eating CPU | mitigate | `ThrottleInterval=10` enforces 10s minimum between restarts (per CONTEXT `### launchd plist`). |
| T-12-03 | Tampering | A malicious / mistaken `__PROJECT_DIR__` resolution could let the agent run outside the project tree | accept | `install-scheduler` uses `Path.cwd()` at invocation time; user runs the command from the project root; cwd log line at startup lets the user verify. Single-user tool, low risk. |
| T-12-04 | Repudiation | Cleanup failure on `.ogg` could leak audio that user thinks is deleted | mitigate | `_cleanup_voice_file` swallows `OSError` but logs WARNING with the path, so the user sees the file in the logs and can purge manually. |
</threat_model>

<verification>
- `uv run pytest -x -q` — full suite (was 468, now ~474-475) passes; new files contribute ~13-15 new tests.
- `uv run ruff check runos tests` — clean (no new lint debt).
- `plutil -lint launchd/com.runos.telegram-bot.plist` exits 0 (run inside `test_committed_template_plutil_lint` and verified locally).
- Smoke-run `uv run runos bot install-scheduler` from a temp data dir — prints a valid plist path + bootstrap/bootout commands + plutil-lint PASS.
- Smoke-run `uv run runos bot purge-voice` with a fake `voice_cache_dir` containing 2 files — prints summary + `Re-run with --yes`; with `--yes` deletes the files.
</verification>

<success_criteria>
- [ ] `launchd/com.runos.telegram-bot.plist` committed, parses, plutil-lint passes; contains `KeepAlive=true`, `ThrottleInterval=10`, `RunAtLoad=true`, `WorkingDirectory=__PROJECT_DIR__`.
- [ ] `runos bot install-scheduler` writes a fully-resolved plist (no `__...__` placeholders left), prints `launchctl bootstrap gui/$(id -u) ...` + `bootout` commands, never invokes `launchctl` itself, validates with `plutil -lint`.
- [ ] `voice_retention_days` Settings field reads `VOICE_RETENTION_DAYS`, default 0, rejects negative values.
- [ ] After a successful voice → agent → reply flow, the cached `.ogg` is deleted by default; with `voice_retention_days>0` the file is kept.
- [ ] Startup sweep removes files older than `voice_retention_days` when > 0; is a no-op when 0; is a no-op when dir doesn't exist.
- [ ] `runos bot purge-voice --yes` deletes everything under `voice_cache_dir`; without `--yes` prints a summary and exits 0 without deleting.
- [ ] Bot startup logs `agent cwd = <abs-path>` on a single INFO line.
- [ ] `.gitignore` ignores `logs/`.
- [ ] Full test suite green; ruff clean.
</success_criteria>

<output>
Create `.planning/phases/12-lifecycle-hardening-privacy/12-01-SUMMARY.md` when done. Note any deviation (e.g. if a fake-Update integration test for the voice handler proved disproportionately hard, what was substituted).
</output>
