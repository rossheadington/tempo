---
phase: 13-strength-tracker
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - tempo/analysis/recovery.py
  - tempo/analysis/runner.py
  - tempo/cli.py
  - tests/test_recovery.py
autonomous: true
requirements:
  - SC-03
  - SC-05

must_haves:
  truths:
    - "`RecoveryAssessment` has new fields `strength: StrengthRollup | None = None` and `strength_present: bool = False` (defaults preserve back-compat with callers that don't pass strength)"
    - "`assess_recovery_from_db(..., strength_path=...)` parses the file, rolls it up to the report's 'as of' day, and attaches the result to a re-constructed (frozen) RecoveryAssessment"
    - "Today-for-strength = `points[-1].day` when points exist, else `date.today()` (mirrors the heat alignment exactly)"
    - "When `strength_path is None` (back-compat), `strength` + `strength_present` default to None/False and the renderer omits the section"
    - "Renderer emits NO `## Strength & conditioning` section when `strength_present=False` OR `strength is None`"
    - "Renderer emits NO section when `strength_present=True` AND `strength.last_session_date is None` (file parsed but zero sessions ever)"
    - "Renderer emits a one-line lapsed nudge (`_S&C protocol lapsed — last session N days ago. (No sessions in the last 28 days.)_`) when all three window counts are zero but `last_session_date is not None`"
    - "Renderer emits the active rollup line (counts / tonnage / last-session-name (N days ago)) when ANY of the three window counts is non-zero"
    - "Tonnage formatting: `9,835 kg` for values < 10,000; `12.4 t` for values ≥ 10,000 (single decimal)"
    - "The Strength section is placed AFTER the Heat section in the rendered report so the two non-running-context blocks cluster together"
    - "`runner.generate_recovery` and `runner.generate_all` accept `strength_path: Path | None = None` and thread it through `assess_recovery_from_db`"
    - "`tempo analyze` and `tempo analyze recovery` CLI subcommands pass `settings.strength_path` next to `settings.heat_path` at both call sites"
  artifacts:
    - path: "tempo/analysis/recovery.py"
      provides: "RecoveryAssessment with strength fields, assess_recovery_from_db accepting strength_path, _render_strength_section helper, _fmt_tonnage helper, render_recovery calling the new section after heat"
      contains: "strength: StrengthRollup | None, strength_present: bool, def _render_strength_section, def _fmt_tonnage, strength_path: Path | None"
    - path: "tempo/analysis/runner.py"
      provides: "generate_recovery and generate_all threading strength_path"
      contains: "strength_path: Path | None"
    - path: "tempo/cli.py"
      provides: "analyze + analyze recovery CLI commands passing settings.strength_path"
      contains: "strength_path=settings.strength_path"
    - path: "tests/test_recovery.py"
      provides: "5 new tests covering the 3-state degradation rule + tonnage formatting + section ordering"
      contains: "test_render_recovery_omits_strength_when_no_strength_file, test_render_recovery_omits_strength_when_present_but_empty, test_render_recovery_renders_strength_when_recent_sessions, test_render_recovery_renders_strength_lapsed_nudge, test_render_recovery_strength_section_follows_heat, test_fmt_tonnage_kg_vs_tonnes"
  key_links:
    - from: "tempo/cli.py::analyze_recovery"
      to: "tempo/analysis/runner.py::generate_recovery"
      via: "passes strength_path=settings.strength_path"
      pattern: "strength_path=settings.strength_path"
    - from: "tempo/analysis/runner.py::generate_recovery"
      to: "tempo/analysis/recovery.py::assess_recovery_from_db"
      via: "passes strength_path through"
      pattern: "strength_path=strength_path"
    - from: "tempo/analysis/recovery.py::render_recovery"
      to: "tempo/analysis/recovery.py::_render_strength_section"
      via: "called after _render_heat_section"
      pattern: "_render_strength_section"
---

<objective>
Wire the strength parser+rollup from Plan 13-01 into the recovery report. Extend `RecoveryAssessment` with `strength` / `strength_present` fields, add a `_render_strength_section` helper that follows the same 3-state degradation rule as heat (absent → omit / lapsed → one-line nudge / active → full rollup line), and thread `strength_path` through `assess_recovery_from_db`, `runner.generate_recovery`, `runner.generate_all`, and both CLI call sites (`tempo analyze`, `tempo analyze recovery`).

The Strength section renders AFTER the Heat section so the two non-running-context blocks cluster together in the recovery report.

Purpose: makes Plan 13-01's parser actually surface in the recovery report — this is the user-observable goal-backward target (SC-03).

Output:
- `tempo/analysis/recovery.py` with `strength` + `strength_present` fields on `RecoveryAssessment`, `_render_strength_section`, `_fmt_tonnage`, and `assess_recovery_from_db` accepting `strength_path`
- `tempo/analysis/runner.py` with `strength_path` threaded through `generate_recovery` + `generate_all`
- `tempo/cli.py` with `strength_path=settings.strength_path` passed at both `runner.*` call sites
- `tests/test_recovery.py` extended with 6 new tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-strength-tracker/13-CONTEXT.md
@.planning/phases/13-strength-tracker/13-01-PLAN.md
@tempo/analysis/recovery.py
@tempo/analysis/runner.py
@tempo/cli.py
@tests/test_recovery.py
@tempo/analysis/strength.py
</context>

<interfaces>
<!-- These are the exports from Plan 13-01 (`tempo/analysis/strength.py`) that this plan consumes. -->
<!-- Executor should use these directly — no need to re-explore strength.py. -->

```python
# From tempo/analysis/strength.py (created in Plan 13-01)

@dataclass(frozen=True, slots=True)
class StrengthRollup:
    today: date
    last_7d_count: int
    last_7d_tonnage_kg: float
    last_14d_count: int
    last_14d_tonnage_kg: float
    last_28d_count: int
    last_28d_tonnage_kg: float
    last_session_date: date | None
    last_session_days_ago: int | None
    last_session_name: str | None  # "unnamed" fallback when session.name was None

@dataclass(frozen=True, slots=True)
class StrengthContext:
    present: bool
    sessions: list[StrengthSession]
    source_path: str | None

def parse_strength(path: Path) -> StrengthContext: ...
def strength_rollup(sessions: list[StrengthSession], today: date) -> StrengthRollup: ...
```
</interfaces>

<preflight_notes>
**KNOWN PITFALL — pre-existing WIP stash on `main`.** A stash currently OFF the tree touches `tempo/bot/`, `tempo/journal/`, `tempo/cli.py`, `tempo/migrations/0005_bot_sessions.sql`, `tempo/transforms/runner.py`, and several `tests/test_bot_*.py`. This plan ALSO touches `tempo/cli.py` (the two `runner.*` call sites at ~lines 611 and 680, both inside `tempo analyze` / `tempo analyze recovery`).

Mitigation:
- BEFORE editing, run `git status` to confirm the stash is NOT applied. If `git stash list` shows the WIP stash, leave it stashed — do NOT pop it.
- Run `git merge origin/main` (or rebase) to ensure the worktree is on the latest committed `main` before editing.
- The CLI edits in this plan are minimal: two single-line additions of `strength_path=settings.strength_path,` next to existing `heat_path=settings.heat_path,` lines. They are surgically scoped and will not conflict with the bot/journal/migration changes in the stash when the maintainer restores it after Phase 13 lands.
- Do NOT touch `tempo/bot/`, `tempo/journal/`, `tempo/migrations/`, `tempo/transforms/runner.py`, or any `tests/test_bot_*.py` — none of those are needed for Phase 13.

**Wave gating.** This plan depends on `13-01` shipping first (the executor must be able to `from tempo.analysis.strength import parse_strength, strength_rollup, StrengthRollup`). Do NOT start this plan until 13-01 is committed to `main`.
</preflight_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend RecoveryAssessment + assess_recovery_from_db with strength fields, add _render_strength_section + _fmt_tonnage</name>
  <files>tempo/analysis/recovery.py</files>
  <read_first>
    - tempo/analysis/recovery.py lines 1-50 (module docstring + imports — add `from tempo.analysis.strength import StrengthRollup`, `from tempo.analysis.strength import strength_rollup as _strength_rollup`, `from tempo.analysis.strength import parse_strength as _parse_strength`, mirroring lines 36-38's heat imports)
    - tempo/analysis/recovery.py lines 232-260 (RecoveryAssessment dataclass with the existing `heat` / `heat_present` fields — `strength` / `strength_present` go next to them)
    - tempo/analysis/recovery.py lines 389-441 (`assess_recovery_from_db` with the heat re-construction at the bottom — the strength block goes AFTER the heat block; the function returns a single re-constructed assessment with BOTH heat and strength attached)
    - tempo/analysis/recovery.py lines 444-500 (`_fmt_days_ago`, `_fmt_minutes`, `_render_heat_section` — `_render_strength_section` mirrors `_render_heat_section`; `_fmt_tonnage` is new, sits near `_fmt_minutes`)
    - tempo/analysis/recovery.py lines 503-558 (`render_recovery` — call `_render_strength_section(out, a.strength, a.strength_present)` immediately AFTER the `_render_heat_section(...)` call at ~line 551)
    - .planning/phases/13-strength-tracker/13-CONTEXT.md `### Recovery report surfacing` section (lines 153-168 — the 3-state rule + tonnage formatting + section ordering)
  </read_first>
  <behavior>
    Imports added (top of file, alongside heat imports at lines 36-38):
    - `from tempo.analysis.strength import StrengthRollup`
    - `from tempo.analysis.strength import parse_strength as _parse_strength`
    - `from tempo.analysis.strength import strength_rollup as _strength_rollup`

    `RecoveryAssessment` (around line 232) gains two new fields right after `heat_present`:
    - `strength: StrengthRollup | None = None`
    - `strength_present: bool = False`

    `_fmt_tonnage(kg: float) -> str` (new helper, near `_fmt_minutes`):
    - `kg < 10_000.0`: `f"{int(round(kg)):,} kg"` (e.g. `"9,835 kg"`)
    - `kg >= 10_000.0`: `f"{kg / 1000.0:.1f} t"` (e.g. `"12.4 t"`)
    - `kg == 0.0`: `"0 kg"`

    `_render_strength_section(out: list[str], strength: StrengthRollup | None, strength_present: bool) -> None` (new, mirrors `_render_heat_section` lines 458-500):
    - `strength_present is False` OR `strength is None` → return (omit section entirely).
    - `strength_present is True` but `strength.last_session_date is None` → return (file parsed, zero sessions ever — omit).
    - `strength_present is True`, `last_session_date is not None`, ALL of `last_7d_count` / `last_14d_count` / `last_28d_count` are 0:
      - Append `"## Strength & conditioning\n"` then a single-line lapsed nudge: `f"_S&C protocol lapsed -- last session {_fmt_days_ago(days_ago)}. (No sessions in the last 28 days.)_"`. Append a trailing `""`.
    - Active state (any non-zero count):
      - Append `"## Strength & conditioning\n"`.
      - Append the active rollup line:
        ```
        - last 7 days: {N} sessions / {tonnage_fmt} tonnage · last 14 days: {N} sessions / {tonnage_fmt} tonnage · last 28 days: {N} sessions / {tonnage_fmt} tonnage · last session: {name} ({days_ago_phrase})
        ```
        where `tonnage_fmt = _fmt_tonnage(strength.last_Nd_tonnage_kg)`, `name = strength.last_session_name or "unnamed"`, `days_ago_phrase = _fmt_days_ago(strength.last_session_days_ago or 0)`.
      - Append a trailing `""`.

    `assess_recovery_from_db` (lines 389-441) signature extended to accept `strength_path: Path | None = None` as the LAST keyword-only argument (after `heat_path`):
    - Function body: keep the existing heat re-construction block; AFTER it (so the final returned assessment carries both heat and strength), add an analogous strength block.
    - The strength block: if `strength_path is None`, skip (return whatever the heat block produced — which may itself be the original assessment if `heat_path` was also None). Otherwise:
      - Compute `today_for_strength` using the SAME alignment rule as heat: try `date.fromisoformat(points[-1].day)` if points exist, else `date.today()`. (Important: don't recompute — reuse the existing `today_for_heat` variable if it's still in scope, otherwise recompute identically.)
      - `strength_ctx = _parse_strength(strength_path)`
      - `strength_roll = _strength_rollup(strength_ctx.sessions, today_for_strength)`
      - Re-construct the (frozen) assessment a SECOND time to carry the strength fields. Preserve every existing field (including `heat` and `heat_present` if they were attached by the heat block). The function ends up doing up to two reconstructions: once for heat (if `heat_path` provided), once for strength (if `strength_path` provided). Refactor option (cleaner): combine the two into a single re-construction at the end of the function — parse both first, then construct once with both attached. The combined approach is preferred; see the implementation note in <action>.
    - Default `strength_path=None` preserves back-compat with any caller that doesn't pass strength (the test suite has such callers).

    `render_recovery` (line ~551): immediately AFTER the existing `_render_heat_section(out, a.heat, a.heat_present)` line, add a single new line: `_render_strength_section(out, a.strength, a.strength_present)`. Same indentation, same shape.
  </behavior>
  <action>
    Implementation order (TDD-friendly):

    1. **Add the imports** at the top of recovery.py (after the existing heat imports at lines 36-38).

    2. **Add the dataclass fields** to `RecoveryAssessment` (after `heat_present: bool = False` at line 253): `strength: StrengthRollup | None = None` then `strength_present: bool = False`.

    3. **Add `_fmt_tonnage`** as a new module-level function near `_fmt_minutes` (line ~453). Match the docstring tone.

    4. **Add `_render_strength_section`** as a new module-level function immediately AFTER `_render_heat_section` (line ~500). Match the docstring tone and the 3-state structure.

    5. **Refactor `assess_recovery_from_db`** (lines 389-441) to handle both heat and strength cleanly. Recommended single-reconstruction pattern:
       ```python
       def assess_recovery_from_db(
           conn,
           *,
           points,
           guardrail,
           window=baselines.DEFAULT_WINDOW,
           min_points=baselines.MIN_POINTS,
           heat_path: Path | None = None,
           strength_path: Path | None = None,
       ) -> RecoveryAssessment:
           latest = baselines.latest_baselines(conn, window=window, min_points=min_points)
           day = points[-1].day if points else None
           assessment = assess_recovery(
               day=day, points=points, guardrail=guardrail, latest_baselines=latest
           )

           if heat_path is None and strength_path is None:
               return assessment

           # Align both tracker windows with the recovery report's "as of" day.
           if points:
               try:
                   today_for_tracker = date.fromisoformat(points[-1].day)
               except ValueError:
                   today_for_tracker = date.today()
           else:
               today_for_tracker = date.today()

           heat_rollup_obj: HeatRollup | None = None
           heat_present = False
           if heat_path is not None:
               heat_ctx = _parse_heat(heat_path)
               heat_rollup_obj = _heat_rollup(heat_ctx.sessions, today_for_tracker)
               heat_present = heat_ctx.present

           strength_rollup_obj: StrengthRollup | None = None
           strength_present = False
           if strength_path is not None:
               strength_ctx = _parse_strength(strength_path)
               strength_rollup_obj = _strength_rollup(strength_ctx.sessions, today_for_tracker)
               strength_present = strength_ctx.present

           return RecoveryAssessment(
               day=assessment.day,
               status=assessment.status,
               rising_load=assessment.rising_load,
               load_reasons=assessment.load_reasons,
               signals=assessment.signals,
               messages=assessment.messages,
               heat=heat_rollup_obj,
               heat_present=heat_present,
               strength=strength_rollup_obj,
               strength_present=strength_present,
           )
       ```
       This collapses the previous two-step reconstruction (heat-only) into one, keeps the alignment-day computed once, and preserves all existing back-compat (heat-only callers still work).

    6. **Update the docstring** of `assess_recovery_from_db` to mention `strength_path` alongside `heat_path` (one short paragraph). Same tone as the existing heat docstring.

    7. **Wire the renderer** at line ~551: add `_render_strength_section(out, a.strength, a.strength_present)` immediately AFTER `_render_heat_section(out, a.heat, a.heat_present)`.

    8. **Do not change** the standalone `assess_recovery` function (the pure version that doesn't touch the DB) — strength integration only flows through the `_from_db` wrapper, exactly mirroring how heat works.
  </action>
  <verify>
    <automated>uv run pytest tests/test_recovery.py -x -v</automated>
    All existing tests still pass; new tests added in Task 4 are not yet present at this point — that's fine, this verifies no regression in the existing heat + recovery render tests.
    Also: `uv run ruff check tempo/analysis/recovery.py` passes.
    Also smoke: `uv run python -c "from tempo.analysis.recovery import RecoveryAssessment, _render_strength_section, _fmt_tonnage; r = RecoveryAssessment(day='2026-05-27', status='ok', rising_load=False, load_reasons=[], signals=[]); assert r.strength is None and r.strength_present is False; print(_fmt_tonnage(9835.0), _fmt_tonnage(12400.0), _fmt_tonnage(0.0))"` → prints `9,835 kg 12.4 t 0 kg`.
  </verify>
  <done>
    `RecoveryAssessment` carries `strength` + `strength_present` (defaults preserve back-compat); `assess_recovery_from_db` accepts `strength_path` and threads it through alongside heat (single reconstruction); `_render_strength_section` and `_fmt_tonnage` exist; `render_recovery` calls the new section after heat; ruff clean; existing recovery tests still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Thread strength_path through runner.generate_recovery and runner.generate_all</name>
  <files>tempo/analysis/runner.py</files>
  <read_first>
    - tempo/analysis/runner.py lines 294-324 (`generate_recovery` signature + body — strength_path goes next to heat_path)
    - tempo/analysis/runner.py lines 366-403 (`generate_all` signature + body — strength_path threaded to the inner `generate_recovery` call)
  </read_first>
  <action>
    1. **`generate_recovery`** (line ~294): add `strength_path: Path | None = None` as a new keyword-only argument immediately after `heat_path: Path` in the signature. Default = None for back-compat with any caller that doesn't pass it.
       - In the body, pass `strength_path=strength_path` into the `recovery_mod.assess_recovery_from_db(...)` call alongside `heat_path=heat_path`.
       - Update the docstring (one short addition) to mention `strength_path` mirrors `heat_path`: "If `strength_path` is provided, the parsed rollup is rendered into a `## Strength & conditioning` section using the same 3-state degradation rule as heat."

    2. **`generate_all`** (line ~366): add `strength_path: Path | None = None` to the signature immediately after `heat_path: Path`. In the body, pass `strength_path=strength_path` into the inner `generate_recovery(...)` call.

    3. No other runner changes — only `generate_recovery` reads strength data, so the threading stops at those two functions.
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q -k "runner or recovery or analyze"</automated>
    Also: `uv run ruff check tempo/analysis/runner.py` passes.
    Also smoke: `uv run python -c "import inspect; from tempo.analysis.runner import generate_recovery, generate_all; assert 'strength_path' in inspect.signature(generate_recovery).parameters; assert 'strength_path' in inspect.signature(generate_all).parameters; print('OK')"`.
  </verify>
  <done>
    Both `generate_recovery` and `generate_all` accept `strength_path: Path | None = None` (defaults preserve back-compat) and thread it through to `assess_recovery_from_db`; ruff clean; no test regressions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pass settings.strength_path at both CLI call sites in tempo/cli.py</name>
  <files>tempo/cli.py</files>
  <read_first>
    - tempo/cli.py lines 595-620 (the `analyze` top-level command — the `runner.generate_all(...)` call at ~line 607 with `heat_path=settings.heat_path` on line 611)
    - tempo/cli.py lines 663-687 (the `analyze recovery` subcommand — the `runner.generate_recovery(...)` call at ~line 677 with `heat_path=settings.heat_path` on line 680)
  </read_first>
  <action>
    1. **`tempo analyze`** (line ~607): in the `runner.generate_all(...)` call, add `strength_path=settings.strength_path,` on the line immediately after `heat_path=settings.heat_path,`. Same indentation, same comma-trailing style. **One single line addition.**

    2. **`tempo analyze recovery`** (line ~677): in the `runner.generate_recovery(...)` call, add `strength_path=settings.strength_path,` on the line immediately after `heat_path=settings.heat_path,`. **One single line addition.**

    3. No other CLI changes. Do NOT add a new `tempo strength` subcommand (out of scope per CONTEXT lines 25-26 — that's deferred Layer 3).

    4. **Pitfall flag (re-read preflight notes)**: if a `git status` or `git stash list` reveals the WIP stash is applied to the tree, abort and ping the maintainer. The stash must be off the tree before these edits land.
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q -k "cli"</automated>
    Also: `uv run ruff check tempo/cli.py` passes.
    Also smoke: `uv run python -c "import subprocess; r = subprocess.run(['uv', 'run', 'tempo', 'analyze', '--help'], capture_output=True, text=True); assert r.returncode == 0; assert 'recovery' in r.stdout; print('OK')"`.
    Also: `grep -c "strength_path=settings.strength_path" tempo/cli.py` returns ≥ 2 (one per call site).
  </verify>
  <done>
    Both CLI call sites (`tempo analyze` and `tempo analyze recovery`) pass `strength_path=settings.strength_path`; ruff clean; no test regressions; CLI still imports + runs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Extend tests/test_recovery.py with 6 new tests covering the strength integration</name>
  <files>tests/test_recovery.py</files>
  <read_first>
    - tests/test_recovery.py lines 280-440 (the heat section rendering tests + `_ok_assessment` builder — copy the shape for strength tests; add a `strength` / `strength_present` kwarg to `_ok_assessment` if it doesn't already accept them, otherwise build a new sibling helper)
  </read_first>
  <behavior>
    Six new tests added to `tests/test_recovery.py` (grouped under a `# ---- Strength & conditioning section ----` divider, immediately after the heat section block at line ~440):

    1. `test_render_recovery_omits_strength_when_no_strength_file`:
       - Build an assessment with `strength=None, strength_present=False`.
       - Render it.
       - Assert `"## Strength & conditioning"` is NOT in the output.

    2. `test_render_recovery_omits_strength_when_present_but_empty`:
       - Build a `StrengthRollup` with `last_session_date=None` (file parsed, zero sessions ever) and `strength_present=True`.
       - Render.
       - Assert `"## Strength & conditioning"` is NOT in the output.

    3. `test_render_recovery_renders_strength_when_recent_sessions`:
       - Build a `StrengthRollup` with `last_7d_count=2, last_7d_tonnage_kg=9835.0, last_14d_count=2, last_14d_tonnage_kg=9835.0, last_28d_count=3, last_28d_tonnage_kg=14200.0, last_session_date=date(2026,5,26), last_session_days_ago=1, last_session_name="Lower body"` and `strength_present=True`.
       - Render.
       - Assert `"## Strength & conditioning"` IS in the output.
       - Assert the active rollup line contains: `"last 7 days: 2 sessions"`, `"9,835 kg"`, `"last 28 days: 3 sessions"`, `"14.2 t"`, `"Lower body"`, `"1 day ago"`.

    4. `test_render_recovery_renders_strength_lapsed_nudge`:
       - Build a `StrengthRollup` with all three window counts = 0 but `last_session_date=date(2026,4,1), last_session_days_ago=56, last_session_name="Upper body"` and `strength_present=True`.
       - Render.
       - Assert `"## Strength & conditioning"` IS in the output.
       - Assert `"S&C protocol lapsed"` and `"56 days ago"` and `"No sessions in the last 28 days"` are all in the output.
       - Assert the active rollup line markers (`"last 7 days:"`, `"tonnage"`) are NOT present (the lapsed nudge is the ONLY strength line).

    5. `test_render_recovery_strength_section_follows_heat`:
       - Build an assessment with BOTH a heat rollup (active) AND a strength rollup (active).
       - Render.
       - Assert both `"## Heat adaptation"` and `"## Strength & conditioning"` are present.
       - Assert the index of `"## Heat adaptation"` is LESS than the index of `"## Strength & conditioning"` in the rendered text (strength comes after heat).

    6. `test_fmt_tonnage_kg_vs_tonnes`:
       - Import `_fmt_tonnage` from `tempo.analysis.recovery`.
       - Assert `_fmt_tonnage(0.0) == "0 kg"`.
       - Assert `_fmt_tonnage(9835.0) == "9,835 kg"`.
       - Assert `_fmt_tonnage(9999.0) == "9,999 kg"`.
       - Assert `_fmt_tonnage(10000.0) == "10.0 t"`.
       - Assert `_fmt_tonnage(12400.0) == "12.4 t"`.

    If the existing `_ok_assessment` helper at line ~284 doesn't accept `strength` / `strength_present` kwargs, extend it to do so (defaults `strength=None, strength_present=False`) so the new tests can call it the same way the heat tests do. Pass-through to the RecoveryAssessment constructor's new kwargs.
  </behavior>
  <action>
    1. Open `tests/test_recovery.py`. Find the `_ok_assessment` helper (line ~284). Extend its signature to accept `strength: object = None, strength_present: bool = False` (mirroring the existing `heat` / `heat_present` kwargs) and pass them through to `RecoveryAssessment(...)`.

    2. Find the heat section tests (line ~341-440). Below the last heat test, add a divider comment: `# ---- Strength & conditioning section ----`.

    3. Write the 6 new tests per the <behavior> spec. Import `StrengthRollup` from `tempo.analysis.strength` at the test-function level (mirror how the heat tests import `HeatRollup`).

    4. The `test_fmt_tonnage_kg_vs_tonnes` test imports `_fmt_tonnage` from `tempo.analysis.recovery` (the helper is module-private by convention but importable for tests, mirroring `_fmt_minutes` if it has its own test).
  </action>
  <verify>
    <automated>uv run pytest tests/test_recovery.py -x -v</automated>
    All existing recovery tests still pass; the 6 new tests pass.
    Also: `uv run pytest tests/ -x --deselect tests/test_bot_transcribe.py::test_transcribe_file_real_fixture_returns_nonempty` — full suite green.
    Also: `uv run ruff check tests/test_recovery.py` passes.
    Also: `grep -c "^def test_render_recovery_omits_strength_when_no_strength_file\|^def test_render_recovery_omits_strength_when_present_but_empty\|^def test_render_recovery_renders_strength_when_recent_sessions\|^def test_render_recovery_renders_strength_lapsed_nudge\|^def test_render_recovery_strength_section_follows_heat\|^def test_fmt_tonnage_kg_vs_tonnes" tests/test_recovery.py` returns 6.
  </verify>
  <done>
    6 new strength tests added; all pass; full suite green; ruff clean; the 3-state degradation rule and the section ordering are verified.
  </done>
</task>

</tasks>

<verification>
**Plan-level checks** (run after all 4 tasks complete):

- `uv run pytest tests/ -x --deselect tests/test_bot_transcribe.py::test_transcribe_file_real_fixture_returns_nonempty` → full suite green
- `uv run ruff check tempo/analysis/recovery.py tempo/analysis/runner.py tempo/cli.py tests/test_recovery.py` → clean
- Smoke (end-to-end against a fake): `uv run python -c "from tempo.analysis.recovery import _render_strength_section, _fmt_tonnage; from tempo.analysis.strength import StrengthRollup; from datetime import date; r = StrengthRollup(today=date(2026,5,27), last_7d_count=1, last_7d_tonnage_kg=9835.0, last_14d_count=1, last_14d_tonnage_kg=9835.0, last_28d_count=1, last_28d_tonnage_kg=9835.0, last_session_date=date(2026,5,26), last_session_days_ago=1, last_session_name='Lower body'); out = []; _render_strength_section(out, r, True); print('\n'.join(out))"` — emits an active rollup line with `9,835 kg` and `Lower body (1 day ago)`.

**Goal-backward check** (proves SC-03):
- A live `tempo analyze recovery` run against a DB + a `strength.md` file containing the owner's Tuesday session must produce a `recovery-YYYY-MM-DD.md` report containing a `## Strength & conditioning` section with the expected counts + tonnage + last-session name. Verifiable post-merge by the maintainer; not part of automated CI.
</verification>

<success_criteria>
- `RecoveryAssessment` has `strength: StrengthRollup | None = None` and `strength_present: bool = False`, defaults preserve back-compat
- `assess_recovery_from_db` accepts `strength_path: Path | None = None`, parses + rolls + attaches via a single reconstruction that also carries the existing heat fields
- `_render_strength_section` honours the 3-state rule (absent → omit / present-but-empty → omit / lapsed → one-line nudge / active → full rollup line)
- `_fmt_tonnage` returns `"9,835 kg"` / `"12.4 t"` / `"0 kg"` per the locked formatting
- The Strength section renders AFTER the Heat section in `render_recovery`
- `runner.generate_recovery` and `runner.generate_all` accept + thread `strength_path` (default None)
- Both CLI sites (`tempo analyze`, `tempo analyze recovery`) pass `settings.strength_path`
- 6 new tests in `tests/test_recovery.py` all pass; full suite green; ruff clean
- No bot/journal/migration/transforms files touched (preflight pitfall respected)
</success_criteria>

<output>
Create `.planning/phases/13-strength-tracker/13-02-SUMMARY.md` when done. Summary must report:
- Confirmation that the recovery report renders the strength section in the expected order (after heat)
- Test count delta in `tests/test_recovery.py` (target: +6)
- Whether the single-reconstruction refactor of `assess_recovery_from_db` was applied (recommended) or two-step (alternative)
- Confirmation that `git status` showed no WIP-stash conflict
- Commit hash for the atomic commit
</output>

<commit_message>
```
feat(13-02): surface strength rollup in the recovery report

Wires the Plan 13-01 strength parser into the recovery report so the
owner's `strength.md` log appears alongside heat in the daily report.

- RecoveryAssessment gains `strength: StrengthRollup | None` and
  `strength_present: bool` fields (defaults preserve back-compat)
- assess_recovery_from_db accepts an optional `strength_path` and uses
  a single re-construction at the end to attach both heat + strength
- New `_render_strength_section` follows the same 3-state degradation
  rule as heat (absent / lapsed / active)
- New `_fmt_tonnage` helper renders `9,835 kg` for <10t, `12.4 t` for ≥10t
- Section is placed AFTER heat in the recovery report so the two
  non-running-context blocks cluster together
- runner.generate_recovery + generate_all thread strength_path through
- CLI: `tempo analyze` and `tempo analyze recovery` both pass
  settings.strength_path next to settings.heat_path
- 6 new tests in tests/test_recovery.py covering the 3 states +
  tonnage formatting + section ordering

SC-03 + SC-05 (recovery-integration portion) addressed.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
</commit_message>
