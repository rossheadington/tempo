---
phase: 14-setup-wizard
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - docs/SETUP.md
  - README.md
autonomous: true
requirements:
  - SETUP-01
  - SETUP-04

must_haves:
  truths:
    - "`docs/SETUP.md` exists at the repo root under `docs/` (mirroring `docs/TELEGRAM_BOT.md`)"
    - "`docs/SETUP.md` documents the one-command path (`runos setup`) AND the full manual fallback (every step a power-user can run by hand) — both work end-to-end"
    - "`docs/SETUP.md` covers the 10 locked steps in CONTEXT order: welcome / tooling-check → db → content → strava → garmin → telegram → scheduler → bot-scheduler → smoke → finish"
    - "Each step section in `docs/SETUP.md` documents: (a) what the step does, (b) what gets prompted for, (c) what files get written / created, (d) how to skip it (`--skip-<step>` or decline-at-prompt), (e) how to recover when it fails"
    - "`docs/SETUP.md` documents the LOCKED flags: `--only=<step>` (and lists the valid step ids), `--skip-garmin`, `--skip-telegram` (with the implies-`--skip-bot-scheduler` note), `--skip-scheduler`, `--skip-bot-scheduler`, `--skip-smoke`, `--non-interactive`"
    - "`docs/SETUP.md` documents how to re-run: `runos setup` (full idempotent re-run, picks up where you left off) AND `runos setup --only=<step>` (re-run a single step, e.g. to rotate Strava creds)"
    - "`docs/SETUP.md` documents the manual uninstall path (3-line `rm` of `~/.runos/`, `.env`, and the two launchd plists with `launchctl bootout` first) per CONTEXT line 28"
    - "`docs/SETUP.md` documents that the wizard NEVER runs `launchctl` itself (the user runs `launchctl bootstrap`) — same convention as `docs/TELEGRAM_BOT.md` and `install-scheduler`"
    - "`docs/SETUP.md` documents the privacy boundary: `.env` is 0600 and gitignored; secrets are never echoed back after entry; voice files are local-only (cross-link `docs/PRIVACY.md`)"
    - "`README.md` 'Getting Started' section leads with `runos setup` as the recommended path (one command)"
    - "`README.md` retains the manual commands (`runos init`, `runos strava auth`, `runos garmin login`, `runos install-scheduler`, `runos bot install-scheduler`) as a 'Manual setup (advanced)' subsection that links to `docs/SETUP.md`"
    - "`README.md` does NOT silently drop any existing setup info — every step previously documented inline either moves to `docs/SETUP.md` or stays in the README's manual-setup fallback"
    - "Markdown renders cleanly (`uv run python -c \"import pathlib; pathlib.Path('docs/SETUP.md').read_text()\"` exits 0; no broken fenced code blocks)"
    - "All cross-links resolve: `docs/SETUP.md` links to `docs/PRIVACY.md`, `docs/TELEGRAM_BOT.md`, `docs/DATE_BUCKETING.md`, `docs/JOURNALING.md`; `README.md` links to `docs/SETUP.md`"
  artifacts:
    - path: "docs/SETUP.md"
      provides: "End-to-end first-run setup walkthrough: one-command path (`runos setup`) + full manual fallback, mirroring `docs/TELEGRAM_BOT.md` structure"
      min_lines: 250
      contains: "# RunOS First-Run Setup, ## One command, ## Manual setup, ### 1. Welcome / tooling check, ### 2. DB init, ### 3. Content directory, ### 4. Strava credentials, ### 5. Garmin login, ### 6. Telegram bot, ### 7. Daily launchd scheduler, ### 8. Telegram-bot launchd scheduler, ### 9. Smoke test, ### 10. Finish, ## Re-running setup, ## Flags reference, ## Recovering from a failed step, ## Uninstall"
    - path: "README.md"
      provides: "Updated Getting Started section that leads with `runos setup` and demotes the manual command list to a fallback subsection"
      contains: "runos setup, docs/SETUP.md"
  key_links:
    - from: "README.md"
      to: "docs/SETUP.md"
      via: "the Getting Started section links to docs/SETUP.md for the full walkthrough"
      pattern: "docs/SETUP\\.md"
    - from: "docs/SETUP.md"
      to: "docs/PRIVACY.md"
      via: "privacy boundary explanation cross-links to the canonical contract"
      pattern: "docs/PRIVACY\\.md"
    - from: "docs/SETUP.md"
      to: "docs/TELEGRAM_BOT.md"
      via: "Telegram bot step deep-dives reference the dedicated bot doc"
      pattern: "docs/TELEGRAM_BOT\\.md"
    - from: "docs/SETUP.md"
      to: ".env.example"
      via: "the variables documented in `.env.example` are the authoritative list; SETUP.md references it for the full set"
      pattern: "\\.env\\.example"
---

<objective>
Write the **documentation layer** for the first-run setup wizard:

1. A new `docs/SETUP.md` end-to-end walkthrough that mirrors `docs/TELEGRAM_BOT.md`'s structure: one-command path (`runos setup`) at the top + full manual fallback (every step a power-user can run by hand) underneath. Each of the 10 LOCKED steps gets its own subsection with what-it-does, what-it-prompts-for, what-files-it-writes, how-to-skip, how-to-recover.

2. A rewritten `README.md` "Getting Started" section that leads with `runos setup` (one command) and demotes the manual command list to a "Manual setup (advanced)" subsection that points at `docs/SETUP.md`.

Purpose: a new user (fresh clone, no setup) reads the README, runs ONE command, and reaches a working `runos run-daily`. A power user who wants to understand every step (or recover from a partial failure) follows the linked `docs/SETUP.md` and gets the full manual.

**Zero source code changes.** This plan only touches markdown. It depends only on Plan 14-01 (env_io + state primitives are committed) for ordering purposes; semantically it documents Plan 14-02's wizard surface. Because Plan 14-02 ships in parallel with this one, the doc must describe the wizard's LOCKED contract (from `14-CONTEXT.md`), not the as-implemented executor-output. If 14-02 deviates from the locked contract, that's a 14-02 bug, not a doc bug.

Output:
- `docs/SETUP.md` (new — ≥ 250 lines)
- `README.md` (modified — Getting Started section rewritten; manual commands preserved as fallback)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-setup-wizard/14-CONTEXT.md
@docs/TELEGRAM_BOT.md
@docs/PRIVACY.md
@README.md
@.env.example
</context>

<preflight_notes>
**Parallel-with-14-02 hygiene.** This plan and Plan 14-02 BOTH ship in Wave 2 with `depends_on: [14-01]`. They have ZERO `files_modified` overlap (14-02 touches `runos/setup/*` + `runos/cli.py` + `tests/test_setup_wizard.py`; 14-03 touches `docs/SETUP.md` + `README.md`). They can run in parallel worktrees and be merged back independently.

**Source-of-truth for the doc.** The locked contract is in `.planning/phases/14-setup-wizard/14-CONTEXT.md` § "Step list (LOCKED, in order)" + "Command shape" + "Re-run safety". DO NOT read Plan 14-02's executor output (`runos/setup/wizard.py`) to write the doc — the doc describes the LOCKED contract regardless of whether 14-02 has shipped yet in this worktree. Doc invariants come from CONTEXT, not from the as-built code.

**Style template.** `docs/TELEGRAM_BOT.md` is the structural model:
- Top: short "what this is + why it exists" paragraph (3-5 lines)
- One-time setup section (the happy path)
- Per-step sections with explicit headers + numbered prompts
- Operational runbook (re-run, recover, uninstall) at the bottom
- Cross-links to other docs at the very end
Match that shape. Do not invent a new layout.

**No code changes.** This plan modifies markdown files only. The executor MUST NOT touch `runos/`, `tests/`, or any `.py` file. If a doc claim turns out to be wrong because Plan 14-02 implemented differently, that's a follow-up — flag it in the SUMMARY, don't patch source from this plan.

**README rewrite scope.** Read the current README's "Getting Started" / setup section before rewriting. The rewrite REPLACES that section but PRESERVES all the previously-documented manual commands as a "Manual setup (advanced)" subsection at the bottom. No information loss. If a section header above or below "Getting Started" needs touching to make the new structure flow, touch the minimum required (per the global CLAUDE.md "surgical changes" rule).
</preflight_notes>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/SETUP.md end-to-end walkthrough</name>
  <files>docs/SETUP.md</files>
  <read_first>
    - .planning/phases/14-setup-wizard/14-CONTEXT.md (the entire file — but especially § "Step list (LOCKED, in order)" lines 51-115, § "Command shape" lines 39-49, § "Re-run safety" lines 148-153, § "Deferred Ideas" lines 232-243 for uninstall guidance)
    - docs/TELEGRAM_BOT.md (the entire file — structural template: short intro / one-time setup / per-step / runbook / cross-links)
    - docs/PRIVACY.md (skim the contract — the SETUP doc cross-links to it but does NOT duplicate it)
    - .env.example (the authoritative list of env vars; the SETUP doc references this file rather than re-listing every variable)
    - README.md (current Getting Started section — to understand what's there today and what needs preserving in Task 2)
  </read_first>
  <behavior>
    Required top-level structure (mirror `docs/TELEGRAM_BOT.md`):

    ```markdown
    # RunOS First-Run Setup

    Single user. Local-first. Mac (for now). This doc walks you from a fresh
    clone to a working `runos run-daily` (and optionally a running Telegram
    voice/text bot) end-to-end.

    There are two paths:

    - **One command (recommended):** `runos setup` — interactive wizard, ~5
      minutes, picks up where it left off if you Ctrl-C.
    - **Manual (advanced):** the same 10 steps run one at a time. Useful if
      you want to understand each layer, or if you're recovering from a
      partial install.

    Both paths produce the same final state: a SQLite DB with the latest
    schema, Strava OAuth complete, optional Garmin login complete, optional
    Telegram bot creds in `.env`, optional launchd jobs in
    `~/Library/LaunchAgents/`, and a successful `runos sync` confirming
    the pipeline works.

    ## One command

    Prerequisites:

    - Python ≥ 3.14 (`python --version`)
    - `uv` on PATH (`brew install uv` if missing)
    - A Strava account and the API page at https://www.strava.com/settings/api ready to receive a new app
    - **Optional:** a Garmin Connect account (for HRV / sleep / resting-HR data)
    - **Optional:** a Telegram account (for the voice/text bot)

    Then:

    ```bash
    git clone https://github.com/rossheadington/RunOS
    cd RunOS
    uv sync                        # install deps into .venv
    uv run runos setup             # run the wizard
    ```

    The wizard walks you through 10 steps. Each step is skippable; each step
    is idempotent (re-running is safe). Press Ctrl-C at any prompt to abort
    — your `.env` and DB are left in a consistent state and a re-run picks
    up where you left off.

    ## Manual setup

    Below is the same flow, step-by-step, for power users.

    ### 1. Welcome / tooling check

    **What it does:** prints a banner, checks Python ≥ 3.14, checks `uv` is
    on PATH, prints a one-line snapshot of what's already installed.

    **Manual equivalent:**

    ```bash
    python --version       # ≥ 3.14
    which uv               # any abs path
    ```

    **Skip:** can't be skipped (always runs; warns rather than fails if `uv`
    is missing — `python -m runos` works fine as a fallback).

    ### 2. DB init

    **What it does:** creates `~/.runos/` (or wherever `RUNOS_DATA_DIR`
    points), opens / creates the SQLite DB in WAL mode, applies all
    migrations up to schema version 5.

    **Wizard prompt:** none in a fresh-install run (skipped with `[done]` if
    the DB is already at the latest schema; offers a confirm re-run only
    under `--only=db`).

    **Files written:** `~/.runos/runos.db` (WAL on, FK on).

    **Manual equivalent:**

    ```bash
    uv run runos init
    ```

    **Skip:** not a sensible skip (the rest of RunOS can't work without the
    DB). The wizard's `[done]` short-circuit handles the "already done" case.

    **Recover:** delete `~/.runos/runos.db` and re-run `runos setup --only=db`.

    ### 3. Content directory

    **What it does:** picks where your markdown trackers live (`races.md`,
    `heat.md`, `strength.md`). Default is `~/.runos/`; the wizard suggests
    `<project>/training/` if it detects you're inside the RunOS source tree.

    **Wizard prompt:** "Content dir path" (default = `~/.runos`).

    **Files written:** `RUNOS_CONTENT_DIR=<chosen>` is appended to `.env`
    via the atomic-write helper (0600 perms). The directory is created with
    0700 perms if it doesn't already exist.

    **Manual equivalent:** edit `.env` and add `RUNOS_CONTENT_DIR=...`; then
    `mkdir -p "$RUNOS_CONTENT_DIR" && chmod 700 "$RUNOS_CONTENT_DIR"`.

    **Skip:** decline the prompt to leave `RUNOS_CONTENT_DIR` unset — the
    default `~/.runos/` is used.

    **Recover:** re-run `runos setup --only=content` to change the choice.

    ### 4. Strava credentials + OAuth

    **What it does:** the only step that requires you to leave the terminal:
    you open https://www.strava.com/settings/api in a browser, create an
    app (callback domain = `localhost`), then paste the Client ID and Client
    Secret back into the wizard. The wizard writes them to `.env` BEFORE
    starting the OAuth handshake (so a partial-OAuth failure leaves the
    creds for retry), then walks you through `authorization_url` →
    "paste the `code` parameter" → `exchange_code`.

    **Wizard prompts:**
    1. `Strava Client ID` (visible)
    2. `Strava Client Secret` (hidden — `getpass`-style; not echoed back)
    3. Browser opens the auth URL automatically (macOS) or prints the URL
       (headless)
    4. `Paste the 'code' parameter from the redirect URL` (visible)

    **Files written:** `RUNOS_STRAVA_CLIENT_ID` + `RUNOS_STRAVA_CLIENT_SECRET`
    appended to `.env` (0600). On successful OAuth, `~/.runos/tokens/strava_tokens.json`
    is created atomically (rotating refresh token persisted).

    **Manual equivalent:**

    ```bash
    # Edit .env to add RUNOS_STRAVA_CLIENT_ID and RUNOS_STRAVA_CLIENT_SECRET, then:
    uv run runos strava auth                       # prints the URL
    uv run runos strava auth --code <CODE>         # completes the handshake
    ```

    **Skip:** you can't sensibly skip Strava — it's the load-bearing data
    source. Decline-and-revisit means re-running `runos setup --only=strava`
    later.

    **Recover:** wizard prints the `runos setup --only=strava` remediation
    on any OAuth failure. The creds you typed are preserved in `.env`, so a
    retry only needs the auth URL → code paste.

    ### 5. Garmin login (optional)

    **What it does:** captures your Garmin email + password (in `.env`,
    0600), then runs the one-time `garmin_login` flow which prompts for an
    MFA code if Garmin asks. On success, Garmin session tokens are persisted
    under `~/.runos/tokens/garmin/`. Every later `runos sync` reuses these
    tokens — no fresh login (this is what keeps you clear of Garmin's
    per-account 429 lockout).

    **Wizard prompts:**
    1. `Do you also use Garmin?` (Y/n, default Y)
    2. `Garmin email` (visible)
    3. `Garmin password` (hidden)
    4. `Garmin MFA code` (visible — prompted only if Garmin requires it)

    **Files written:** `RUNOS_GARMIN_EMAIL` + `RUNOS_GARMIN_PASSWORD`
    appended to `.env`; `~/.runos/tokens/garmin/` directory created with
    session tokens.

    **Manual equivalent:**

    ```bash
    # Edit .env to add RUNOS_GARMIN_EMAIL and RUNOS_GARMIN_PASSWORD, then:
    uv run runos garmin login
    ```

    **Skip:** `runos setup --skip-garmin`, or decline the wizard's first
    Garmin prompt. RunOS runs fine on Strava-only data; some analyses (HRV
    z-score, resting-HR trend, sleep correlation) degrade to "insufficient
    data" without Garmin.

    **Recover:** if `garmin_login` fails repeatedly with 429s, STOP. Wait at
    least a few hours. Garmin's per-account lockout compounds with every
    login attempt. Re-run `runos setup --only=garmin` only after the wait.

    ### 6. Telegram bot credentials (optional)

    **What it does:** captures the Telegram bot token (from @BotFather) and
    your numeric chat id (from @userinfobot), writes them to `.env`.
    Optionally lets you tweak the Whisper model defaults and the voice-file
    retention window.

    **Wizard prompts:**
    1. `Do you want to run the Telegram voice/text bot?` (y/N, default N)
    2. Instruction block: @BotFather + @userinfobot steps printed verbatim
    3. `Telegram bot token` (hidden)
    4. `Your Telegram chat id (numeric)` (visible)
    5. `Change Whisper model defaults?` (y/N, default N) — if Y, prompts
       for `WHISPER_MODEL_NAME` / `WHISPER_COMPUTE_TYPE` / `WHISPER_DEVICE`
    6. `Change voice retention?` (y/N, default N) — if Y, prompts for
       `VOICE_RETENTION_DAYS`

    **Files written:** `TELEGRAM_BOT_TOKEN` + `TELEGRAM_OWNER_CHAT_ID`
    appended to `.env` (note: these are BARE names, not `RUNOS_`-prefixed
    — see `.env.example`). Optional Whisper / voice-retention keys are
    appended only if you explicitly opted to change them.

    **Manual equivalent:** see `docs/TELEGRAM_BOT.md` for the full
    @BotFather + @userinfobot walkthrough.

    **Skip:** `runos setup --skip-telegram` (implies
    `--skip-bot-scheduler` — the bot launchd step is only offered when
    Telegram is configured).

    **Recover:** re-run `runos setup --only=telegram`. Existing values are
    shown as `[set]` with a keep/change/skip choice.

    ### 7. Daily-sync launchd scheduler (optional)

    **What it does:** renders `~/Library/LaunchAgents/com.runos.daily.plist`
    with absolute paths to `uv`, the project root, and the IANA timezone.
    `runos run-daily` then fires every day at the chosen local hour:minute
    (via `StartCalendarInterval`, which — unlike cron — runs a MISSED job
    on wake).

    **Wizard prompts:**
    1. `Install the daily-sync launchd job?` (Y/n, default Y)
    2. `Hour (0-23)` (default 5)
    3. `Minute (0-59)` (default 30)

    **Files written:** `~/Library/LaunchAgents/com.runos.daily.plist` (the
    plist is rendered from a committed template).

    **RunOS does NOT load the plist for you.** It prints the
    `launchctl bootstrap` line you need to run yourself — running launchctl
    is an explicit human step (the same convention as
    `docs/TELEGRAM_BOT.md`):

    ```bash
    launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.runos.daily.plist
    launchctl enable gui/$(id -u)/com.runos.daily
    ```

    To unload later:

    ```bash
    launchctl bootout gui/$(id -u)/com.runos.daily
    ```

    **Manual equivalent:** `uv run runos install-scheduler --to-launch-agents --hour 5 --minute 30`.

    **Skip:** `runos setup --skip-scheduler`. You can still run
    `uv run runos run-daily` by hand any time.

    **Recover:** delete `~/Library/LaunchAgents/com.runos.daily.plist` (after
    `bootout`) and re-run `runos setup --only=scheduler`.

    ### 8. Telegram-bot launchd scheduler (optional)

    **What it does:** renders
    `~/Library/LaunchAgents/com.runos.telegram-bot.plist` with
    `KeepAlive=true` so the bot survives crashes / sleep/wake / reboots.

    **Wizard prompts:**
    1. `Install the Telegram-bot launchd job?` (Y/n, default Y)

    **Offer condition:** this step is OFFERED ONLY IF the Telegram step
    completed in this run, OR Telegram was already configured before this
    run started. Skipping or declining the Telegram step automatically
    skips this one (CONTEXT-locked).

    **Files written:**
    `~/Library/LaunchAgents/com.runos.telegram-bot.plist`.

    **Launchctl invocation (you run this, not the wizard):**

    ```bash
    launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.runos.telegram-bot.plist
    launchctl enable gui/$(id -u)/com.runos.telegram-bot
    launchctl kickstart -k gui/$(id -u)/com.runos.telegram-bot
    ```

    **Manual equivalent:** `uv run runos bot install-scheduler --to-launch-agents`.

    **Skip:** `runos setup --skip-bot-scheduler`, or (implicitly)
    `--skip-telegram`.

    **Recover:** delete the plist (after `bootout`) and re-run
    `runos setup --only=bot-scheduler`. See `docs/TELEGRAM_BOT.md` for the
    full operational runbook.

    ### 9. Smoke test

    **What it does:** runs `runos sync` (Strava-then-isolated-Garmin)
    IN-PROCESS — not as a subprocess — and prints a per-source status line:

    ```
    Strava: ✓ (42 raw rows)
    garmin: skipped -- 429
    ```

    A Garmin skip is NEVER terminal (Garmin is an isolated failure domain,
    per project convention). A terminal Strava failure (auth-error, not
    rate-limit) causes the wizard to exit with code 1 and points you at
    `runos setup --only=strava` for recovery.

    **Wizard prompts:** none.

    **Files written:** rows into `raw_response` (whatever the sync pulls).

    **Manual equivalent:** `uv run runos sync`.

    **Skip:** `runos setup --skip-smoke`.

    **Recover:** if Strava failed terminally, follow the remediation
    printed by the wizard. If Garmin skipped with a 429, wait a few hours
    and run `runos garmin sync` by hand.

    ### 10. Finish

    **What it does:** prints a summary mirroring the welcome banner
    (`Strava ✓ · Garmin ✓ · Telegram ✗ ...`) plus next-step hints: where
    your latest report lives, how to load the launchd plists, how to
    test the Telegram bot, where to find this doc.

    ## Re-running setup

    `runos setup` is idempotent. You can re-run it any time:

    - **Full re-run:** `runos setup`. Steps already done print `[done]`
      and are skipped. Steps not yet done prompt as usual. Steps in a
      partial state (creds set but no token, say) prompt with `[set]
      keep / change / fresh`.
    - **Single step:** `runos setup --only=<step>`. Re-runs only the
      named step. Valid: `db`, `content`, `strava`, `garmin`, `telegram`,
      `scheduler`, `bot-scheduler`, `smoke`. Stackable: `--only=telegram
      --only=bot-scheduler` runs both.

    Typical re-run scenarios:

    - Add the Telegram bot to an existing Strava-only install:
      `runos setup --only=telegram --only=bot-scheduler`
    - Rotate Strava creds: `runos setup --only=strava`
    - Move your content dir: `runos setup --only=content`
    - Re-run the smoke test after fixing a network issue:
      `runos setup --only=smoke`

    ## Flags reference

    | Flag | Effect |
    |------|--------|
    | `--only=<step>` | Run only the named step. Stackable. |
    | `--skip-garmin` | Skip the Garmin login step. |
    | `--skip-telegram` | Skip Telegram bot setup. Implies `--skip-bot-scheduler`. |
    | `--skip-scheduler` | Skip daily launchd install. |
    | `--skip-bot-scheduler` | Skip bot launchd install (but still set up bot creds). |
    | `--skip-smoke` | Skip the final `runos sync` smoke test. |
    | `--non-interactive` | Fail fast on any prompt (testing-only). |

    Exit codes: 0 = success, 1 = a non-skipped step failed terminally,
    2 = Ctrl-C / Abort / `--non-interactive` triggered.

    ## Recovering from a failed step

    Every step in the wizard is recoverable. The pattern:

    1. Read the error message + the remediation hint the wizard printed.
    2. Re-run with `--only=<step>` for the failing step.
    3. If a credential is wrong, the wizard will re-prompt; type the
       correct value. The previous bad value is overwritten atomically.

    A few specific recoveries:

    - **Strava OAuth failed** (code expired, network blip): re-run
      `runos setup --only=strava`. The client_id + client_secret you
      typed earlier are still in `.env` — the wizard re-prompts only
      for the missing pieces (the auth URL + the new `code`).
    - **Garmin login 429**: STOP. Wait at least a few hours. Garmin's
      per-account lockout compounds. Do NOT loop logins.
    - **Launchd plist won't load**: usually a `plutil -lint` issue; run
      `plutil -lint ~/Library/LaunchAgents/com.runos.daily.plist` to see
      the error. The wizard runs `plutil -lint` automatically on render
      so this should be rare.

    ## Privacy boundary

    - `.env` is 0600 and gitignored. The wizard's atomic-write helper
      enforces 0600 on every write.
    - Secret values entered at hidden-input prompts (Strava client
      secret, Garmin password, Telegram bot token) are NEVER echoed back
      to the terminal after entry, and are NEVER logged.
    - Voice files (if you set up the Telegram bot) are transcribed
      locally via `faster-whisper` — audio bytes never leave the laptop.
      Transcripts + Claude Code tool calls flow through your existing
      Claude subscription. See `docs/PRIVACY.md` for the full contract.

    ## Uninstall

    The wizard does not have an uninstall mode (deferred — single-user
    local tool, manual `rm` is fine). The 4-line manual uninstall:

    ```bash
    # Stop the launchd jobs (if installed) FIRST so they don't hold files open.
    launchctl bootout gui/$(id -u)/com.runos.daily || true
    launchctl bootout gui/$(id -u)/com.runos.telegram-bot || true

    # Remove the plists.
    rm -f ~/Library/LaunchAgents/com.runos.daily.plist
    rm -f ~/Library/LaunchAgents/com.runos.telegram-bot.plist

    # Remove the data dir (DB, tokens, reports, voice cache).
    rm -rf ~/.runos/

    # Remove the .env (creds).
    rm -f .env
    ```

    ## Related docs

    - `docs/TELEGRAM_BOT.md` — full Telegram bot setup + operational runbook
    - `docs/PRIVACY.md` — the authoritative privacy contract
    - `docs/DATE_BUCKETING.md` — local-date attribution rule (relevant if you're debugging a wrong-date activity)
    - `docs/JOURNALING.md` — the journal-entry contract for Claude / the bot
    - `.env.example` — authoritative list of every env var RunOS reads
    ```

    Notes for the executor:

    - Mirror `docs/TELEGRAM_BOT.md` tone — terse, opinionated, no
      marketing voice. "You" and "the wizard" are fine. "The user" is
      fine in lists. Avoid emoji except for the literal ✓/✗ in the
      install summary (matches the wizard's output).
    - The per-step subsections are repetitive on purpose. Each one
      stands on its own — a power user reading just one section gets the
      complete recipe for that step.
    - Keep the doc under ~500 lines. The structure above hits ~280-350.
    - Markdown only. No HTML. Code fences use bash for shell snippets;
      no language tag for plain output.
    - File MUST end with a trailing newline.
  </behavior>
  <action>
    1. Create `docs/SETUP.md`. Use the structure above verbatim — it IS the doc, just expand where literal step prompts are mentioned (use the prompts copy from CONTEXT lines 67-99) and confirm command lines match the existing CLI surface (`runos init`, `runos strava auth`, `runos strava auth --code <CODE>`, `runos garmin login`, `runos install-scheduler --to-launch-agents`, `runos bot install-scheduler --to-launch-agents`).
    2. Cross-link `docs/PRIVACY.md`, `docs/TELEGRAM_BOT.md`, `docs/DATE_BUCKETING.md`, `docs/JOURNALING.md`, `.env.example` at the bottom.
    3. Verify line count is ≥ 250.
    4. Verify the Markdown renders cleanly: no unmatched fences, no broken tables.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib; p = pathlib.Path('docs/SETUP.md'); content = p.read_text(); assert len(content.splitlines()) >= 250, f'too short: {len(content.splitlines())} lines'; assert content.endswith('\\n'), 'must end with newline'; print(f'OK ({len(content.splitlines())} lines)')"</automated>
    Also: section presence check — every required header must appear:
    ```bash
    uv run python -c "
    import pathlib
    content = pathlib.Path('docs/SETUP.md').read_text()
    required = [
        '# RunOS First-Run Setup',
        '## One command',
        '## Manual setup',
        '### 1. Welcome',
        '### 2. DB init',
        '### 3. Content directory',
        '### 4. Strava credentials',
        '### 5. Garmin login',
        '### 6. Telegram bot',
        '### 7. Daily',
        '### 8. Telegram-bot launchd',
        '### 9. Smoke test',
        '### 10. Finish',
        '## Re-running setup',
        '## Flags reference',
        '## Recovering from a failed step',
        '## Privacy boundary',
        '## Uninstall',
        '## Related docs',
    ]
    missing = [r for r in required if r not in content]
    assert not missing, f'missing headers: {missing}'
    print('all 19 required sections present')
    "
    ```
    Also: fenced-code-block balance check — count of ` ``` ` lines must be even:
    ```bash
    uv run python -c "
    import pathlib
    fences = pathlib.Path('docs/SETUP.md').read_text().count('\\n\\`\\`\\`')
    assert fences % 2 == 0, f'unbalanced code fences: {fences} occurrences'
    print(f'fence balance OK ({fences} delimiters)')
    "
    ```
    Also: cross-link check — required link targets must appear:
    ```bash
    uv run python -c "
    import pathlib
    content = pathlib.Path('docs/SETUP.md').read_text()
    for link in ['docs/PRIVACY.md', 'docs/TELEGRAM_BOT.md', 'docs/DATE_BUCKETING.md', 'docs/JOURNALING.md', '.env.example']:
        assert link in content, f'missing link: {link}'
    print('cross-links OK')
    "
    ```
  </verify>
  <done>
    `docs/SETUP.md` exists with ≥ 250 lines, all 19 required section headers present, balanced fenced code blocks, all 5 cross-links present, ends with a trailing newline, mirrors `docs/TELEGRAM_BOT.md`'s structure.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md Getting Started to lead with `runos setup`</name>
  <files>README.md</files>
  <read_first>
    - README.md (entire file — find the existing Getting Started / setup section to understand what's currently there and what must be preserved)
    - docs/SETUP.md (Task 1's output — the README's new Getting Started section links to it)
    - .planning/phases/14-setup-wizard/14-CONTEXT.md § "What this phase delivers" lines 12-21 (the contract for the README rewrite)
  </read_first>
  <behavior>
    The README currently documents the manual setup commands inline (assumption based on CLAUDE.md and project structure — read it to confirm before rewriting). The new Getting Started section follows this shape:

    ```markdown
    ## Getting Started

    Single user. Local-first. Mac (for now). ~5 minutes from clone to a
    working daily-sync pipeline.

    ### One command (recommended)

    ```bash
    git clone https://github.com/rossheadington/RunOS
    cd RunOS
    uv sync
    uv run runos setup
    ```

    The interactive wizard walks you through DB init → Strava OAuth →
    optional Garmin login → optional Telegram bot → optional launchd jobs
    → smoke `runos sync`. Idempotent: re-runs pick up where you left off.

    See `docs/SETUP.md` for the full end-to-end walkthrough (every step,
    every prompt, every flag, recovery paths, uninstall).

    ### Re-running a single step

    ```bash
    uv run runos setup --only=telegram          # add the bot to an existing install
    uv run runos setup --only=strava            # rotate Strava creds
    uv run runos setup --skip-garmin            # full run, no Garmin
    ```

    ### Manual setup (advanced)

    If you prefer to run each step by hand (or you're debugging a stuck
    install), the equivalent commands are:

    ```bash
    # Edit .env with RUNOS_STRAVA_CLIENT_ID + RUNOS_STRAVA_CLIENT_SECRET first
    # (see .env.example for the full variable list).

    uv run runos init                                   # DB schema
    uv run runos strava auth                            # prints OAuth URL
    uv run runos strava auth --code <CODE>              # completes handshake

    # Optional: Garmin
    uv run runos garmin login

    # Optional: Telegram bot (see docs/TELEGRAM_BOT.md for the full walkthrough)
    uv run runos bot run                                # foreground; launchd for background

    # Optional: launchd
    uv run runos install-scheduler --to-launch-agents --hour 5 --minute 30
    uv run runos bot install-scheduler --to-launch-agents

    # Smoke
    uv run runos sync
    ```

    Full details + per-step recovery: `docs/SETUP.md`.
    ```

    Editing rules:

    - REPLACE the existing Getting Started / setup section with the new
      structure above. Do NOT touch other sections (Overview, Architecture,
      Constraints, Day-to-day, etc.) unless a header above or below the
      Getting Started section needs adjusting for flow.
    - PRESERVE every manual command that previously appeared in the README
      — they all live in the new "Manual setup (advanced)" subsection.
    - If the existing README already has a different setup section header
      (e.g. "Setup" or "Installation"), keep its header text + just rewrite
      the body. Match the surrounding style.
    - Per CLAUDE.md "surgical changes" rule: every changed line should
      trace to the requirement "lead with `runos setup`, preserve manual
      commands as fallback, link to docs/SETUP.md". Do not "improve"
      adjacent prose, fix unrelated typos, or reformat code blocks
      elsewhere.
  </behavior>
  <action>
    1. Read the current `README.md` to find the existing setup / Getting Started section. Identify its exact line range.
    2. Replace ONLY that section with the new structure above. Keep the same top-level header style as the surrounding sections (i.e. if other sections are `## Foo`, use `## Getting Started`; if they're `### Foo`, drop a level).
    3. Confirm every manual command that previously appeared anywhere in the README still appears somewhere (either in the new section or untouched elsewhere). NO information loss.
    4. Add the cross-link to `docs/SETUP.md` at the bottom of the new section.
    5. Verify no other section was touched.
  </action>
  <verify>
    <automated>uv run python -c "
    import pathlib
    content = pathlib.Path('README.md').read_text()
    required_phrases = [
        'runos setup',
        'docs/SETUP.md',
        'runos strava auth',
        'runos garmin login',
        'runos install-scheduler',
        'runos bot install-scheduler',
        'Manual setup',
        'uv sync',
    ]
    missing = [p for p in required_phrases if p not in content]
    assert not missing, f'missing: {missing}'
    print(f'README OK ({len(content.splitlines())} lines)')
    "</automated>
    Also: confirm `runos setup` appears BEFORE `runos init` (the new flow leads with the wizard):
    ```bash
    uv run python -c "
    import pathlib
    content = pathlib.Path('README.md').read_text()
    pos_setup = content.find('runos setup')
    pos_init = content.find('runos init')
    assert pos_setup != -1 and pos_init != -1, 'both must be present'
    assert pos_setup < pos_init, f'runos setup at {pos_setup} should come before runos init at {pos_init}'
    print('ordering OK')
    "
    ```
    Also: git diff sanity — only README.md changed:
    ```bash
    git diff --name-only | grep -v -E '(^docs/SETUP\\.md$|^README\\.md$|^\\.planning/)' | wc -l
    ```
    Expected: `0` (no source code changes).
  </verify>
  <done>
    `README.md` Getting Started leads with `runos setup`, retains all previously-documented manual commands in a "Manual setup (advanced)" subsection, links to `docs/SETUP.md`, no information loss elsewhere in the README, no source code changes in this plan's commit.
  </done>
</task>

</tasks>

<verification>
**Plan-level checks** (run after both tasks complete):

- `docs/SETUP.md` exists with ≥ 250 lines and all 19 required section headers
- `README.md` Getting Started leads with `runos setup` and links to `docs/SETUP.md`
- Every manual command previously documented in README is still present (either in README or in `docs/SETUP.md`)
- `git diff --name-only` shows ONLY `docs/SETUP.md` and `README.md` (plus the planning artifacts directory) — zero `runos/` or `tests/` files
- `uv run pytest tests/ -x --deselect tests/test_bot_transcribe.py::test_transcribe_file_real_fixture_returns_nonempty` → green (no test changes, no regressions)
- Markdown lints clean (no obviously-broken fences, no orphan headers)

**Goal-backward check** (proves SETUP-01 + SETUP-04 documentation portion):
- The 10-step walkthrough in `docs/SETUP.md` documents the same 10 LOCKED steps the wizard runs (SETUP-01).
- Each step section's "Manual equivalent" subsection names the EXISTING CLI command being delegated to — proving SETUP-04 (delegation) end-to-end from the user's perspective.
</verification>

<success_criteria>
- `docs/SETUP.md` exists, ≥ 250 lines, mirrors `docs/TELEGRAM_BOT.md` structure
- All 10 LOCKED steps documented in order with what / prompts / files-written / skip / recover sections
- All 7 LOCKED flags (`--only`, `--skip-garmin`, `--skip-telegram`, `--skip-scheduler`, `--skip-bot-scheduler`, `--skip-smoke`, `--non-interactive`) documented with effect
- Re-run guidance + uninstall guidance + privacy boundary all present
- Cross-links to `docs/PRIVACY.md`, `docs/TELEGRAM_BOT.md`, `docs/DATE_BUCKETING.md`, `docs/JOURNALING.md`, `.env.example` all present
- `README.md` Getting Started leads with `runos setup`, preserves manual commands as a fallback subsection, links to `docs/SETUP.md`
- Zero source code changes (no `runos/` or `tests/` files in the diff)
- Full test suite still green
</success_criteria>

<output>
Create `.planning/phases/14-setup-wizard/14-03-SUMMARY.md` when done. Summary must report:
- Line count of `docs/SETUP.md` (target: 280-400)
- Line count of `README.md` before / after the rewrite (target: net delta within ±50 lines — preserved info, just restructured)
- Confirmation that every required section header is present in `docs/SETUP.md` (list the 19)
- Confirmation that every manual command previously in the README is still present somewhere (README or `docs/SETUP.md`)
- Confirmation that `git diff --name-only` shows ONLY markdown files (no .py changes)
- Commit hash for the atomic commit
</output>

<commit_message>
```
docs(14-03): add docs/SETUP.md walkthrough + lead README with `runos setup`

The doc layer for Phase 14's first-run setup wizard. No source code
changes; this commit only adds the user-facing documentation that
describes how to use the wizard shipped in Plan 14-02.

- `docs/SETUP.md` (new, ~300 lines) -- end-to-end first-run walkthrough
  mirroring docs/TELEGRAM_BOT.md's structure. One-command path
  (`runos setup`) at the top + full manual fallback underneath. Each of
  the 10 LOCKED steps gets its own subsection with what-it-does,
  what-it-prompts-for, what-files-it-writes, how-to-skip, and how-to-
  recover. Flags reference table covers all 7 locked flags. Re-run
  guidance, recovery paths, privacy boundary, manual uninstall, and
  cross-links to PRIVACY.md / TELEGRAM_BOT.md / DATE_BUCKETING.md /
  JOURNALING.md / .env.example all included.
- `README.md` -- Getting Started section rewritten to lead with
  `runos setup` as the recommended path. Every manual command
  previously documented inline is preserved in a "Manual setup
  (advanced)" subsection that links to docs/SETUP.md for the full
  walkthrough. No information loss.

SETUP-01, SETUP-04 (docs portion) addressed.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
</commit_message>
</content>
</invoke>